# Backlog — PsiAgente

Visão consolidada do backlog de novas funcionalidades, em 2 grupos. Ao
implementar um item, mova-o de "A realizar" para "Implementado" e registre a
data. Detalhamento de escopo/decisões de cada item está em
`docs/backlog-novas-funcionalidades.md`; histórico técnico de cada entrega em
`docs/status-implementacao.md`.

## Implementado

| Item | Descrição | Data | Observações |
| --- | --- | --- | --- |
| 1 | Blog de psicologia e saúde mental (`/blog`, `blog.psiagente.com.br`, CRUD `/admin/artigos`) | 2026-08-03 | CRUD ainda não testado clicando no formulário do navegador |
| 3 | Painel administrativo + cadastro de profissionais (convite + autocadastro) | 2026-08-03 (convite) / 2026-08-04 (autocadastro) | Fluxo de convite ainda não verificado ponta a ponta em produção; sem gate funcional por `aprovado` (depende do item 2) |
| 4 | Landing page para tráfego pago — Google Ads (`comece.psiagente.com.br`) | 2026-08-04 | Redesenhada com a identidade PsiAgente em 2026-08-06; no ar em produção |
| 2 | Diretório público de psicólogos (`busca.psiagente.com.br`: listagem/filtro, perfil individual, contato via WhatsApp) + CTA de cadastro, Termos de Uso obrigatórios, barreira de qualidade e ferramentas de divulgação | 2026-08-03/04 | Perfil só aparece publicamente quando `aprovado` (item 3) e `visivel_diretorio = true`; fluxo de convite do item 3 ainda não verificado ponta a ponta em produção. Redesenho visual (marketplace, cards de perfil, hero) em 2026-08-13 |
| 5 | Importar pacientes via planilha Excel, com tela de mapeamento de colunas | 2026-08-04 | Inclui também o campo "Precisa de recibo" no cadastro de paciente (fora do escopo original do item, surgiu na mesma sessão) e o filtro correspondente em `/recibos`. Estendido em 2026-08-11 pra aceitar CPF/RG. Campo renomeado pro item 6 abaixo |
| 1b | Papel de "criador de conteúdo" separado de admin (evolução pedida do item 1) | 2026-08-06 | — |
| 11 (metade 1) | Planos do produto (Psi Gestão / Psi Gestão + Marketing / Psi Marketing) — modelo de plano e controle de acesso, atribuição manual pelo admin em `/admin/profissionais` | 2026-08-13 | Sem cobrança/gateway (fora de escopo, ver item 11 completo abaixo). Código mesclado e pushado; verificação end-to-end no navegador ainda pendente (ferramentas de browser desconectadas na sessão) |
| 6 | Diferenciar Recibo de Nota Fiscal no cadastro do paciente — campo `Paciente.documento` (Nenhum/Receita Saúde/Nota Fiscal) substitui o boolean `precisa_recibo` | 2026-08-13 (verificação end-to-end em 2026-08-14) | `/recibos` continua só lidando com Receita Saúde (Nota Fiscal ainda não tem emissão real — depende do item 7). Verificado ponta a ponta contra produção (build + preview local via Playwright CLI): cadastro/edição do select Documento, filtro de `/recibos` (recibo aparece, nota_fiscal não, "Gerar recibo" funciona) e mapeamento automático da coluna "Documento" no import por planilha |
| 8 | Gerar arquivo TXT do movimento de atendimentos "Recibo" pro Carnê-Leão, no layout "Recibos do Receita Saúde" (16 campos) — tela `/carne-leao` com seletor de mês, agrupamento por pagador+paciente e opção de combinar múltiplos atendimentos pagos juntos numa única linha/recibo. Inclui nova tela `/configuracoes/conta` pro profissional cadastrar o próprio CPF/CRP (pré-requisito, campo `Usuarios.cpf` novo) | 2026-08-13 | Fonte de dados é `PagamentoSessao` (valor/data realmente recebidos), não `Sessao`. CPF validado (11 dígitos) antes de entrar no arquivo; pagamento sem CPF válido fica de fora com aviso. Geração sempre recalculada no servidor a partir dos ids (nunca confia em valor/CPF vindo do client). Verificação end-to-end no navegador ainda pendente |
| 9 | Rotina periódica (semanal/quinzenal/mensal) de envio automático do TXT do Carnê-Leão por e-mail, via n8n externo (implantado na mesma VPS) chamando um endpoint novo (`/carne-leao-automatico`, protegido por segredo compartilhado) que decide quem está na data, gera o conteúdo e devolve pro n8n distribuir | 2026-08-14 | Configuração de frequência/e-mail de destino em `/configuracoes/conta`. Evita duplicidade/lacuna sem depender do item 10: cobre o delta desde o último envio, nunca cruzando virada de mês. Nova tabela `EnvioAutomaticoCarneLeao` (auditoria, bloqueada com RLS). **Pendências de infra pra funcionar em produção**: configurar `CARNE_LEAO_CRON_SECRET` no EasyPanel e no nó HTTP do n8n (documentado em `docs/status-implementacao.md`); implantar o workflow do n8n em si (fora do escopo de código). Verificação end-to-end no navegador ainda pendente |
| 7 | Emitir Nota Fiscal (NFS-e) direto pelo sistema — microserviço Python novo (`nfse-service/`, vendoriza o kit `NotaFiscal/nfse-nacional-kit`) + telas `/configuracoes/nfse` (dados fiscais, certificado A1, troca homologação→produção irreversível) e `/notas-fiscais` (emissão e cancelamento), com envio automático por e-mail via Resend | 2026-08-14 (infra e teste com certificado real em 2026-08-15) | Self-service pra qualquer profissional. `NotaFiscal` fonte de dados é `PagamentoSessao` (valor realmente pago), mesmo padrão do item 8. Sem gerador de PDF próprio nesta v1 (fallback é "tentar de novo depois"). Serviço `psiagente-nfse` implantado na VPS (fora do EasyPanel, deploy manual via Docker Swarm). Testado com certificado A1 real de um profissional contra a SEFIN homologação: certificado aceito, DPS assinada e validada corretamente pela SEFIN (schema/regras fiscais OK) — única rejeição foi `E0008` (SEFIN reportando horário no futuro), que persistiu com folgas de até 2h no timestamp, indicando instabilidade do ambiente de homologação da SEFIN naquele momento, não bug no código. **Ainda falta**: configurar Resend (e-mail automático falha silenciosamente até lá — não bloqueia emissão) e confirmar que uma emissão real é aceita quando o ambiente da SEFIN normalizar |
| 12 | Segmento "Anamnese" no cadastro do paciente (medicação, médico responsável, tempo de terapia, tempo com este profissional, queixas iniciais e evolução, e outros campos clínicos de prontuário) | 2026-08-15 | Campos clínicos numa aba própria (`/pacientes/[id]?aba=anamnese`) e histórico versionado por followup: cada salvamento gera um evento append-only só com os campos que mudaram (mais observação livre opcional), sem duplicar os que ficaram iguais nem gerar evento quando nada muda. Verificado ponta a ponta contra produção (build + preview local via Playwright CLI, dados descartáveis): criação, edição parcial, salvar sem mudança (nenhum evento novo) e observação isolada (evento só com a observação) — todos bateram com o esperado. RLS confirmada por PostgREST direto (sessão de um profissional não enxerga paciente/anamnese de outro). Sem teste automatizado (projeto não tem framework de testes) |
| 13 (metade 1) | Agente de WhatsApp — 5 RPCs novas (`agent_reagendar_sessao`, `agent_excluir_sessao`, `agent_excluir_pagamento`, `agent_registrar_lancamento_despesa`, `agent_registrar_anamnese`), seguindo o mesmo padrão de segurança das 11 já existentes | 2026-08-17 | Verificado com dados descartáveis contra produção: contador/alerta de reagendamento, bloqueio de exclusão com vínculo financeiro, exclusão de pagamento desfazendo o lançamento, lançamento de despesa recusando conta de outro usuário, e os 4 cenários de anamnese (criação/edição parcial/no-op/observação isolada) |
| 13 (metade 2) | Agente de WhatsApp — 3 workflows n8n (`WA - Enviar Mensagem`, `WA - Agent Psicólogo`, `WA - Inbound Router`) ligando o WhatsApp (Evolution API) às 12+5 RPCs, com logging em `agent_audit_log` | 2026-08-24 (revisão final de branch); teste real ponta a ponta pelo usuário em 2026-08-25 | Webhook autenticado por header (achado crítico da revisão final, corrigido). Transcrição de áudio (Whisper/entrada multimodal) explicitamente adiada a pedido do usuário — hoje mensagem de voz recebe resposta fixa "por enquanto só consigo entender mensagens de texto". Canal do paciente e relatórios (resumo/PDF) seguem fora de escopo desta entrega |
| 14 | Link público pro paciente completar o próprio cadastro — profissional gera/envia um link de dentro do app, paciente preenche sem precisar de login | 2026-08-25 | Proposta fica pendente sem prazo até o profissional revisar; se rejeitada, ainda pode ser aceita dentro de uma janela de 60 dias; aceite é campo a campo (checklist com tudo marcado por padrão). Verificado ponta a ponta contra produção (build + preview local via Playwright CLI, dados descartáveis): gerar link e abrir sem login com dados pré-preenchidos, enviar proposta e ver aviso na ficha, aceitar desmarcando um campo (só o campo marcado muda), rejeitar e depois aceitar mesmo assim dentro do prazo, link já usado/expirado mostrando "Link inválido", e segundo link invalidando o primeiro |
| 16 | Classificação Financeira (plano de contas simplificado) — nova entidade `ClassificacaoFinanceira` (Receita/Despesa/Ambos) vinculável a qualquer lançamento, com tela própria (`/financeiro/classificacoes`) — e despesas recorrentes (`RecorrenciaDespesa`), mesmo padrão de "cron preguiçoso" já usado nas recorrências de sessão, com geração automática até um horizonte de 3 meses ao abrir `/financeiro` | 2026-08-26 | Filtro por classificação em `/financeiro/lancamentos`; campo "Repetir" (Semanal/Quinzenal/Mensal) só aparece na criação de uma Despesa, nunca na edição. Cancelar uma recorrência desativa a série e remove os lançamentos futuros ainda não realizados, preservando o histórico. Revisão de branch inteira pegou e corrigiu 3 achados (série de recorrência podia ficar órfã se o lançamento inicial falhasse, classificação não era validada contra o tipo Receita/Despesa, `revalidatePath` faltando em `/financeiro`). Verificado ponta a ponta contra produção com dados descartáveis: 14 checks via RLS/SQL direto (matemática de recorrência, geração até o horizonte, idempotência, cancelamento) + 7 checks de UI via Playwright (listagens, toggle do campo "Repetir"); a submissão de formulário via navegador ficou bloqueada por um bug pré-existente e não relacionado (cookie de sessão não é reenviado em POST de Server Action neste ambiente — reproduzido também no formulário de Conta Financeira, já existente). **Extensão (2026-08-26, PR #3)**: lista padrão de 19 classificações comuns de consultório (`web/lib/classificacoes-padrao.js`), carregada automaticamente no autocadastro e no convite pelo admin, e sob demanda via botão "Carregar lista padrão" em `/financeiro/classificacoes` — cada profissional tem sua própria lista, carregar nunca duplica (constraint única `owner+nome`, upsert `ON CONFLICT DO NOTHING`, verificado sob concorrência real) |
| 20 | Ajuste de responsividade para celular — cards de lista (Pacientes, Financeiro, Recibos, Notas Fiscais, Consultórios, Pacotes, Recorrências, Admin) empilham em coluna no mobile; Agenda semana vira lista de cards de largura total no mobile (só usa scroll horizontal a partir de tablet); Agenda mês perde o texto detalhado no mobile (vira indicador por bolinha) e corrige um bug de truncamento que estourava a célula; grids fixos (Financeiro, Painel, detalhe do paciente) viram responsivos | 2026-08-28 | Verificado com Playwright em 375×812 (mobile) e 1366×900 (confirma que o desktop não mudou), com dados de teste descartáveis (revertidos ao final). Cabeçalhos "título + botão" em todo o app ganharam `flex-wrap` como rede de segurança contra telas muito estreitas (320px) |
| 24 (parte 1) | CRM próprio de vendas do SaaS — parte 1: notificação por e-mail ao ADM (`carlosventto@gmail.com`) a cada cadastro novo (`cadastrar()` em `web/lib/actions/auth.js`) e a cada contrato fechado (webhook Asaas, branch `pendenciaLiberada` em `web/app/api/asaas/webhook/route.js` — só dispara na ativação de um plano pago, não em renovação mensal de rotina) | 2026-09-12 | Reaproveita o helper `enviarEmailResend` já existente (`web/lib/email/resend.js`), sem template novo. Nenhum dos dois gatilhos bloqueia o fluxo principal se o Resend falhar. Verificado ponta a ponta com dados descartáveis contra produção: envio direto via Resend confirmado (HTTP 200), e simulação de evento `PAYMENT_CONFIRMED` sintético contra o webhook local com usuário de teste confirmou a transição de plano exatamente no branch onde o e-mail é disparado — usuário e evento de teste apagados ao final |
| 24 (parte 2) | CRM próprio de vendas do SaaS — parte 2: rastreamento de visita ao site — tabela `VisitaPagina` (host/path/UTM/referrer/hash de IP, sem RLS com policy, mesmo padrão de `EventoAssinatura`) + cookie de sessão anônima `pv_id` (1 ano) capturado em `web/proxy.js`, cobrindo os 3 subdomínios públicos inteiros e o funil pré-login do domínio principal (`/`, `/login`, `/cadastro`, `/completar-cadastro`, `/esqueci-senha`); `Usuarios.visitante_sessao_id` liga o cadastro de volta ao funil de páginas vistas | 2026-09-12 | IP nunca gravado em texto puro, só hash SHA-256. Não rastreia nada do painel logado. Exclui prefetch especulativo (`Next-Router-Prefetch`) mas conta navegação real via RSC. Verificado ponta a ponta com dados descartáveis contra produção: cookie criado/reusado corretamente, pageview gravado em `/`, `/login` e no subdomínio `comece.` (com UTM), `/agenda` (autenticado) corretamente não gravado — dado de teste apagado ao final. Achado um bug pré-existente não relacionado (`/cadastro` trava sob requisição de prefetch sintética em `next start`), registrado em `.claude/challenges/2026-09-12-prefetch-cadastro-trava.md`, não investigado a fundo por estar fora do escopo |
| 24 (parte 3) | CRM próprio de vendas do SaaS — parte 3: schema (`Lead`/`LeadNota`, RLS admin-only via `public.is_admin()`) + tela `/admin/leads` (lista, filtro por estágio, criação manual, detalhe com histórico de notas). Toda conta nova gera um Lead automático (`origem: "cadastro"`), linkado a `visitante_sessao_id` quando existir. `/admin/leads` antigo (referência estática do CFP) move pra `/admin/leads-cfp` | 2026-09-12 | Verificado com dados descartáveis contra produção: RLS confirmada nos dois sentidos (admin le/escreve, não-admin não le nem escreve), fluxo completo criar→mudar estágio→nota→ler funcionando via sessão real, insert automático via service-role confirmado no schema. **Não verificado clicando na UI de verdade** — chrome-devtools MCP não conectou nesta sessão; validação ficou na camada de dados/RLS (onde estava o risco real), mas os componentes de tela (`LeadManualForm`, `SeletorEstagioLead`, `LeadNotaForm`, as duas páginas) ainda não foram exercitados num navegador |
| 10 | Marcar atendimento como "já gerado em TXT" no Carnê-Leão — novo campo `PagamentoSessao.carne_leao_gerado_em`; geração manual (`/carne-leao`) continua listando tudo mas avisa (`confirm()`) antes de gerar de novo um já marcado, com botão "Desmarcar"; geração automática (`/carne-leao-automatico`) nunca inclui um pagamento já marcado, sem perguntar | 2026-08-26 | Marcação acontece no momento em que o TXT é efetivamente montado (manual e automático), não na seleção. Verificado ponta a ponta contra produção com dado real preexistente (pagamento id 9, paciente de teste "TESTE"): query estilo manual continua trazendo o pagamento com `carne_leao_gerado_em` preenchido após marcar, query estilo automática passa a excluí-lo, e desmarcar restaura o estado original — dado revertido ao final do teste. Verificação de UI via navegador (badge/botão) não executada — bloqueada pelo mesmo bug pré-existente de cookie em POST de Server Action já registrado no item 16 (a ação "Desmarcar" também é uma Server Action) |

## A realizar

| Item | Descrição | Depende de |
| --- | --- | --- |
| 11 (metade 2) | Cobrança/gateway de pagamento dos planos (preço, assinatura, inadimplência) | 11 (metade 1) |
| 7 (infra) | Configurar conta Resend em produção (`RESEND_API_KEY`/`RESEND_FROM_EMAIL` no app principal, domínio de envio verificado) pra habilitar o e-mail automático da NFS-e ao paciente — hoje falha silenciosamente sem bloquear a emissão | 7 |
| 15 | Integração com Open Finance bancário — importar lançamentos da conta do profissional, identificar recebimentos de pacientes e vincular automaticamente com pagamentos de sessão | — |
| 17 | Confirmação de escopo ao alterar horário/dia de uma sessão recorrente (só esta sessão ou todas as futuras) | — |
| 18 | Registrar motivação da alteração de sessão (paciente ou profissional) | 17 |
| 19 | Gráficos de indicadores de mercado na tela inicial | — |
| 21 | Contadores de valor previsto e quantidade na Agenda, de acordo com o filtro de período (dia/semana/mês) | — |
| 22 | Liberação temporária de plano avançado pelo admin (período de teste sem cobrança) | 11 (metade 1) |
| 24 (parte 4) | CRM próprio de vendas do SaaS — parte 4: agente comercial autônomo via WhatsApp (número/instância própria, separada da do agente que atende paciente) pra tirar dúvida de possível cliente (psicólogo interessado em assinar), com handoff pro ADM assumir a conversa quando o agente não resolver | 24 (parte 3) |
| 23 | Diferencial real do plano Psi Marketing no diretório público: (a) prioridade de exibição em `/busca` pra perfis de plano pago (hoje ordena só por `criado_em desc`, sem distinção de plano) e (b) conteúdo tipo stories no perfil (foto/vídeo com expiração, ao estilo Instagram) — schema com expiração, upload de vídeo, visualizador na página pública, rotina de limpeza do que expirou, e revisão de regras do CFP sobre autopromoção antes de ir ao ar. Pedido surgido ao revisar o texto de preços da home institucional (2026-09-12): hoje o plano Grátis já tem `temDiretorio: true`, então "aparecer no diretório" não é diferencial de fato do Marketing — precisa de algo que o Grátis não tenha. Tratar como projeto próprio, com brainstorm dedicado (schema/storage/moderação) antes de codar. | — |

Detalhamento de cada item em `docs/backlog-novas-funcionalidades.md`. Ver também
`docs/status-implementacao.md` pra funcionalidades feitas fora do backlog original
(rebrand PsiAgente, documentos/dependente financeiro do paciente, excluir/desativar
paciente, cartão de usuário logado na sidebar).
