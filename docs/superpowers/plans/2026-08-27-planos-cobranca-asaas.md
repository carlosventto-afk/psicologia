# Planos e Cobrança (Asaas) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Cobrar de verdade pelos 3 planos pagos do PsiAgente via Asaas, introduzir um plano Grátis com recursos limitados, e ajustar o agente de WhatsApp pra não depender mais de "consultório ativo".

**Architecture:** Checkout hospedado do Asaas (zero dado de cartão no nosso servidor) + webhook autenticado por token pra confirmar pagamento/inadimplência + um job diário (reaproveitando a infra de cron externo do item 9) pra aplicar trocas de plano que não têm nenhum webhook associado (downgrade pra Grátis, carência esgotada). `Usuarios.plano` continua sendo o único campo que os gates de UI olham.

**Tech Stack:** Next.js 16 (App Router, Server Actions), Supabase (Postgres + RLS), Asaas REST API v3, n8n (scripts em `scripts/n8n-agente-whatsapp/`).

**Spec:** `docs/superpowers/specs/2026-08-27-planos-cobranca-asaas-design.md`

## Global Constraints

- Preços exatos: Grátis R$0, `gestao` R$49,90, `gestao_marketing` R$79,90, `marketing` R$39,90 (mensal, sem anual nesta v1).
- Projeto não tem framework de testes automatizados — toda verificação é feita com scripts Node descartáveis (usando `pg`/`@supabase/supabase-js` de `web/node_modules`) rodados contra produção com dado descartável, seguindo o padrão já usado nos itens 10/13/16. Nenhum script de verificação é commitado — apagar ao final de cada task.
- `web/lib/supabase/admin.js` (`createAdminClient()`) só pode ser usado em código que já verificou explicitamente que quem chama tem permissão — nunca exposto direto a partir de uma ação sem esse check antes (convenção já documentada no próprio arquivo).
- Não mexer em `(app)/(gestao)/layout.js` além do necessário — o redirect de `plano === 'marketing'` pra `/diretorio` já existe e não muda.
- Nomes de tabela em PascalCase entre aspas duplas (`"Usuarios"`, `"Consultorio"` etc.) — convenção já estabelecida no schema.

---

## Task 1: Migration — colunas de assinatura em `Usuarios` + tabela `EventoAssinatura` + trava de coluna

Esta task também fecha uma lacuna de segurança pré-existente: a policy `usuarios_self` (`supabase/migrations/20260727000003_enable_rls_policies.sql:103`) é `for all` sem nenhuma restrição de coluna — hoje, qualquer profissional logado já consegue, via uma chamada direta à API REST do Supabase (fora do nosso app), setar o próprio `plano` pra `'gestao_marketing'` de graça. Isso é inofensivo hoje (sem cobrança por trás), mas vira uma brecha financeira real assim que `plano` passa a gatear recursos pagos de verdade.

**Files:**
- Create: `supabase/migrations/20260827000001_add_assinatura_usuarios.sql`

**Interfaces:**
- Produces: colunas `Usuarios.plano_pago`, `Usuarios.plano_pretendido`, `Usuarios.plano_pretendido_a_partir_de`, `Usuarios.assinatura_status`, `Usuarios.asaas_customer_id`, `Usuarios.asaas_subscription_id`, `Usuarios.assinatura_vencida_em`; `Usuarios.plano` aceita `'gratis'` e tem default `'gratis'`; tabela `"EventoAssinatura"`.

- [ ] **Step 1: Escrever a migration**

```sql
-- supabase/migrations/20260827000001_add_assinatura_usuarios.sql

alter table "Usuarios"
  add column plano_pago text check (plano_pago in ('gestao', 'gestao_marketing', 'marketing')),
  add column plano_pretendido text check (plano_pretendido in ('gratis', 'gestao', 'gestao_marketing', 'marketing')),
  add column plano_pretendido_a_partir_de timestamptz,
  add column assinatura_status text check (assinatura_status in ('ativa', 'inadimplente', 'cancelada')),
  add column asaas_customer_id text,
  add column asaas_subscription_id text,
  add column assinatura_vencida_em timestamptz;

alter table "Usuarios" drop constraint usuarios_plano_check;
alter table "Usuarios" add constraint usuarios_plano_check
  check (plano in ('gratis', 'gestao', 'gestao_marketing', 'marketing'));

alter table "Usuarios" alter column plano set default 'gratis';

-- Fecha a lacuna: a policy "usuarios_self" e' row-level (for all, sem
-- restricao de coluna) -- sem isto, qualquer profissional logado
-- conseguiria setar o proprio plano/assinatura via uma chamada direta a
-- API REST do Supabase, sem passar pelo checkout do Asaas. So o
-- service_role (usado pelo webhook, pelo job diario, e pela acao de admin
-- corrigida na Task 2) pode escrever essas colunas.
revoke update (
  plano, plano_pago, plano_pretendido, plano_pretendido_a_partir_de,
  assinatura_status, asaas_customer_id, asaas_subscription_id, assinatura_vencida_em
) on "Usuarios" from authenticated;

create table "EventoAssinatura" (
  id bigint generated by default as identity primary key,
  created_at timestamptz not null default now(),
  usuario bigint references "Usuarios"(id),
  asaas_event_id text not null unique,
  tipo text not null,
  payload jsonb not null,
  processado_com_sucesso boolean not null default true,
  mensagem_erro text
);

alter table "EventoAssinatura" enable row level security;
-- sem nenhuma policy: so service_role (que ignora RLS) le/escreve aqui,
-- mesmo padrao ja usado em "EnvioAutomaticoCarneLeao".
```

- [ ] **Step 2: Aplicar a migration em produção**

```bash
node -e "
const { Client } = require('pg');
const fs = require('fs');
(async () => {
  const client = new Client({ connectionString: \`postgresql://postgres:\${process.env.SUPABASE_DB_PASSWORD}@db.rohulajgyxdangxfurha.supabase.co:5432/postgres\` });
  await client.connect();
  await client.query(fs.readFileSync('supabase/migrations/20260827000001_add_assinatura_usuarios.sql', 'utf-8'));
  console.log('OK: migration aplicada');
  await client.end();
})().catch((e) => { console.error(e.message); process.exit(1); });
"
```

- [ ] **Step 3: Verificar — colunas existem, default correto, e a trava de coluna funciona**

Rodar como script Node descartável (apagar depois):

```js
// scratch-verificar-migration-assinatura.js
const fs = require('fs');
function loadEnv(file) {
  const env = {};
  for (const line of fs.readFileSync(file, 'utf-8').split('\n')) {
    const m = line.match(/^([A-Z_]+)=(.*)$/);
    if (m) env[m[1]] = m[2].trim();
  }
  return env;
}
const env = loadEnv('web/.env.local');
const { createClient } = require('./web/node_modules/@supabase/supabase-js');
const { Client } = require('pg');

async function main() {
  // 1) colunas + default, via conexao direta (service tem acesso irrestrito ao catalogo)
  const pg = new Client({ connectionString: `postgresql://postgres:${process.env.SUPABASE_DB_PASSWORD}@db.rohulajgyxdangxfurha.supabase.co:5432/postgres` });
  await pg.connect();
  const { rows: colDefault } = await pg.query(`
    select column_default from information_schema.columns
    where table_name = 'Usuarios' and column_name = 'plano'
  `);
  console.log('Default de Usuarios.plano:', colDefault[0].column_default, '(esperado: algo com gratis)');

  // 2) a trava de coluna bloqueia update via role "authenticated"
  const { rows: grantCheck } = await pg.query(`
    select has_column_privilege('authenticated', '"Usuarios"', 'plano', 'UPDATE') as pode_atualizar
  `);
  console.log('authenticated pode atualizar Usuarios.plano?', grantCheck[0].pode_atualizar, '(esperado: false)');
  await pg.end();

  // 3) EventoAssinatura existe e aceita insert via service_role
  const admin = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY);
  const { error } = await admin.from('EventoAssinatura').insert({
    asaas_event_id: 'teste-verificacao-migration',
    tipo: 'teste',
    payload: { teste: true },
  });
  console.log('Insert de teste em EventoAssinatura:', error ? `FALHOU: ${error.message}` : 'OK');
  if (!error) {
    await admin.from('EventoAssinatura').delete().eq('asaas_event_id', 'teste-verificacao-migration');
  }
}
main();
```

Rodar: `node scratch-verificar-migration-assinatura.js`
Esperado: default menciona `gratis`, `pode_atualizar` é `false`, insert em `EventoAssinatura` OK. Depois apagar o arquivo (`rm scratch-verificar-migration-assinatura.js`).

- [ ] **Step 4: Commit**

```bash
git add supabase/migrations/20260827000001_add_assinatura_usuarios.sql
git commit -m "feat: adiciona colunas de assinatura em Usuarios e tabela EventoAssinatura"
```

---

## Task 2: Corrigir `alterarPlano` pra usar `service_role`

A Task 1 revogou `UPDATE` em `Usuarios.plano` da role `authenticated` — isso quebra `alterarPlano` (`web/lib/actions/profissionais.js:97-107`), que hoje escreve `plano` via `createClient()` (client de sessão, role `authenticated`). Precisa passar a usar `createAdminClient()`, com o mesmo padrão de "checar admin explicitamente antes" já usado em `convidarProfissional` no mesmo arquivo.

**Files:**
- Modify: `web/lib/actions/profissionais.js:97-107`

**Interfaces:**
- Consumes: `createAdminClient` de `@/lib/supabase/admin` (já existe).
- Produces: `alterarPlano(id, novoPlano)` continua com a mesma assinatura — nenhum chamador precisa mudar.

- [ ] **Step 1: Reescrever a função**

```js
export async function alterarPlano(id, novoPlano) {
  const supabase = await createClient();

  const {
    data: { user },
  } = await supabase.auth.getUser();

  if (!user) {
    throw new Error("Não autorizado.");
  }

  const { data: usuarioAtual, error: erroUsuarioAtual } = await supabase
    .from("Usuarios")
    .select("role")
    .eq("id_user", user.id)
    .single();

  if (erroUsuarioAtual || usuarioAtual?.role !== "admin") {
    throw new Error("Não autorizado.");
  }

  const admin = createAdminClient();
  const { error } = await admin.from("Usuarios").update({ plano: novoPlano }).eq("id", id);

  if (error) {
    throw new Error(error.message);
  }

  revalidatePath("/admin/profissionais");
}
```

- [ ] **Step 2: Verificar que o build passa**

Rodar: `cd web && npm run build`
Esperado: build conclui sem erro (o `createAdminClient` já está importado no topo do arquivo, usado por `convidarProfissional`).

- [ ] **Step 3: Verificar contra produção**

```js
// scratch-verificar-alterar-plano.js — rodar de dentro de web/, node com acesso a web/node_modules
const fs = require('fs');
function loadEnv(file) {
  const env = {};
  for (const line of fs.readFileSync(file, 'utf-8').split('\n')) {
    const m = line.match(/^([A-Z_]+)=(.*)$/);
    if (m) env[m[1]] = m[2].trim();
  }
  return env;
}
const env = loadEnv('.env.local');
const { createClient } = require('./node_modules/@supabase/supabase-js');

async function main() {
  const admin = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY);
  // Confirma que a coluna plano so aceita escrita via service_role agora
  // (mesma checagem da Task 1, so repetida aqui pra validar que o app
  // ainda consegue escrever via admin client apos a mudanca de codigo).
  const { data: usuarioTeste } = await admin.from('Usuarios').select('id, plano').limit(1).single();
  const planoOriginal = usuarioTeste.plano;
  const { error } = await admin.from('Usuarios').update({ plano: planoOriginal }).eq('id', usuarioTeste.id);
  console.log('Update via service_role (mesmo caminho que alterarPlano agora usa):', error ? `FALHOU: ${error.message}` : 'OK');
}
main();
```

Rodar: `cd web && node ../scratch-verificar-alterar-plano.js`
Esperado: `OK`. Apagar o script depois.

- [ ] **Step 4: Commit**

```bash
git add web/lib/actions/profissionais.js
git commit -m "fix: alterarPlano passa a usar service_role (coluna plano trancada pra authenticated)"
```

---

## Task 3: `web/lib/planos.js` — metadados centralizados dos planos

**Files:**
- Create: `web/lib/planos.js`

**Interfaces:**
- Produces: `export const PLANOS` (objeto indexado por id de plano), `export function temAcessoPago(usuario)`.

- [ ] **Step 1: Escrever o arquivo**

```js
// web/lib/planos.js
export const PLANOS = {
  gratis: {
    id: "gratis",
    nome: "Grátis",
    preco: 0,
    temGestao: true,
    temDiretorio: true,
    temDocumentos: false,
    temWhatsapp: false,
    temCarneLeao: false,
    limiteConsultorios: 1,
  },
  gestao: {
    id: "gestao",
    nome: "Psi Gestão",
    preco: 49.9,
    temGestao: true,
    temDiretorio: false,
    temDocumentos: true,
    temWhatsapp: true,
    temCarneLeao: true,
    limiteConsultorios: null,
  },
  gestao_marketing: {
    id: "gestao_marketing",
    nome: "Psi Gestão + Marketing",
    preco: 79.9,
    temGestao: true,
    temDiretorio: true,
    temDocumentos: true,
    temWhatsapp: true,
    temCarneLeao: true,
    limiteConsultorios: null,
  },
  marketing: {
    id: "marketing",
    nome: "Psi Marketing",
    preco: 39.9,
    temGestao: false,
    temDiretorio: true,
    temDocumentos: false,
    temWhatsapp: false,
    temCarneLeao: false,
    limiteConsultorios: null,
  },
};

export function temAcessoPago(usuario) {
  return usuario.plano !== "gratis";
}
```

- [ ] **Step 2: Verificar**

```bash
node -e "const { PLANOS, temAcessoPago } = require('./web/lib/planos.js'); console.log(Object.keys(PLANOS)); console.log(temAcessoPago({ plano: 'gratis' }), temAcessoPago({ plano: 'gestao' }));"
```

Esperado: `[ 'gratis', 'gestao', 'gestao_marketing', 'marketing' ]` e depois `false true`. (Se o `require` falhar por ser ESM, rodar via `node --input-type=module -e "import('./web/lib/planos.js').then(m => {...})"` — checar o `package.json` do projeto pra confirmar se usa `type: module`; o restante do código usa `import`/`export` então adaptar o comando de verificação conforme necessário, mas o conteúdo do arquivo em si não muda.)

- [ ] **Step 3: Commit**

```bash
git add web/lib/planos.js
git commit -m "feat: centraliza metadados dos planos (preco, recursos, limite de consultorio)"
```

---

## Task 4: Estender `buscarUsuarioAtual()` com as colunas novas

**Files:**
- Modify: `web/lib/data/usuario.js:10-14`

**Interfaces:**
- Produces: objeto retornado por `buscarUsuarioAtual()` ganha `plano_pago`, `plano_pretendido`, `plano_pretendido_a_partir_de`, `assinatura_status`, `asaas_customer_id`, `asaas_subscription_id`.

- [ ] **Step 1: Editar o select**

```js
  const { data, error } = await supabase
    .from("Usuarios")
    .select(
      "id, nome, cpf, crp, contato, whatsapp_number, whatsapp_verified, role, aprovado, criador_conteudo, plano, plano_pago, plano_pretendido, plano_pretendido_a_partir_de, assinatura_status, asaas_customer_id, asaas_subscription_id, carne_leao_frequencia, carne_leao_email, carne_leao_ultimo_envio"
    )
    .eq("id_user", user.id)
    .single();
```

- [ ] **Step 2: Verificar que o build passa**

Rodar: `cd web && npm run build`
Esperado: sem erro (nenhum outro código depende do formato exato do select).

- [ ] **Step 3: Commit**

```bash
git add web/lib/data/usuario.js
git commit -m "feat: buscarUsuarioAtual traz os campos novos de assinatura"
```

---

## Task 5: `web/lib/asaas.js` — cliente da API do Asaas

**Pré-requisito externo:** precisa de `ASAAS_API_KEY` (env var, chave de API de uma conta Asaas real ou sandbox) pra qualquer chamada de verificação funcionar de verdade. O código desta task compila e é usado pelas tasks seguintes independente disso; só a *verificação com chamada real* (Step 3) depende da chave existir.

**Files:**
- Create: `web/lib/asaas.js`

**Interfaces:**
- Produces: `criarClienteAsaas({ nome, cpf })`, `criarAssinaturaAsaas({ customerId, value, description })`, `buscarAssinaturaAsaas(subscriptionId)`, `atualizarAssinaturaAsaas(subscriptionId, { value })`, `cancelarAssinaturaAsaas(subscriptionId)` — todas `async`, todas lançam `Error` em caso de falha da API.

- [ ] **Step 1: Escrever o arquivo**

```js
// web/lib/asaas.js
const ASAAS_API_URL = process.env.ASAAS_API_URL || "https://api.asaas.com/v3";

function headers() {
  return {
    "Content-Type": "application/json",
    access_token: process.env.ASAAS_API_KEY,
  };
}

async function asaasFetch(path, options = {}) {
  const resposta = await fetch(`${ASAAS_API_URL}${path}`, {
    ...options,
    headers: { ...headers(), ...options.headers },
  });
  const dados = await resposta.json();
  if (!resposta.ok) {
    throw new Error(dados.errors?.[0]?.description || `Erro na API do Asaas (${resposta.status})`);
  }
  return dados;
}

export async function criarClienteAsaas({ nome, cpf }) {
  return asaasFetch("/customers", {
    method: "POST",
    body: JSON.stringify({ name: nome, cpfCnpj: String(cpf ?? "").replace(/\D/g, "") }),
  });
}

export async function criarAssinaturaAsaas({ customerId, value, description }) {
  return asaasFetch("/subscriptions", {
    method: "POST",
    body: JSON.stringify({
      customer: customerId,
      billingType: "UNDEFINED",
      cycle: "MONTHLY",
      value,
      description,
      nextDueDate: new Date().toISOString().slice(0, 10),
    }),
  });
}

export async function buscarAssinaturaAsaas(subscriptionId) {
  return asaasFetch(`/subscriptions/${subscriptionId}`);
}

export async function atualizarAssinaturaAsaas(subscriptionId, { value }) {
  return asaasFetch(`/subscriptions/${subscriptionId}`, {
    method: "PUT",
    body: JSON.stringify({ value }),
  });
}

export async function cancelarAssinaturaAsaas(subscriptionId) {
  return asaasFetch(`/subscriptions/${subscriptionId}`, { method: "DELETE" });
}
```

- [ ] **Step 2: Adicionar as env vars ao `.env.local`**

```bash
cd web
cat >> .env.local << 'EOF'
ASAAS_API_URL=https://api.asaas.com/v3
ASAAS_API_KEY=
ASAAS_WEBHOOK_TOKEN=
EOF
```

Deixar `ASAAS_API_KEY`/`ASAAS_WEBHOOK_TOKEN` vazias até o usuário fornecer a conta Asaas — sem elas, `criarClienteAsaas`/`criarAssinaturaAsaas` vão falhar com erro da API (esperado, não é bug).

- [ ] **Step 3: Verificar (só roda de verdade com `ASAAS_API_KEY` preenchida)**

```js
// scratch-verificar-asaas.js
const fs = require('fs');
function loadEnv(file) {
  const env = {};
  for (const line of fs.readFileSync(file, 'utf-8').split('\n')) {
    const m = line.match(/^([A-Z_]+)=(.*)$/);
    if (m) env[m[1]] = m[2].trim();
  }
  return env;
}
Object.assign(process.env, loadEnv('web/.env.local'));

async function main() {
  const { criarClienteAsaas, criarAssinaturaAsaas, buscarAssinaturaAsaas, cancelarAssinaturaAsaas } = await import('./web/lib/asaas.js');

  const cliente = await criarClienteAsaas({ nome: 'Teste PsiAgente', cpf: '00000000000' });
  console.log('Cliente criado:', cliente.id);

  const assinatura = await criarAssinaturaAsaas({ customerId: cliente.id, value: 1, description: 'Teste de verificação' });
  console.log('Assinatura criada:', assinatura.id, 'checkout:', assinatura.invoiceUrl ?? assinatura.paymentLink);

  const buscada = await buscarAssinaturaAsaas(assinatura.id);
  console.log('Busca confere:', buscada.id === assinatura.id);

  await cancelarAssinaturaAsaas(assinatura.id);
  console.log('Assinatura de teste cancelada.');
}
main().catch((e) => { console.error('ERRO:', e.message); process.exit(1); });
```

Rodar: `node scratch-verificar-asaas.js` (só depois de preencher `ASAAS_API_KEY` em `web/.env.local`). Esperado: cliente e assinatura criados, busca confere, cancelamento sem erro. Apagar o script depois.

- [ ] **Step 4: Commit**

```bash
git add web/lib/asaas.js
git commit -m "feat: adiciona cliente da API do Asaas (customers/subscriptions)"
```

Nota: **não** commitar `.env.local` (já deve estar no `.gitignore` — confirmar com `git status` antes do commit desta task).

---

## Task 6: Página `/assinatura` — escolher/trocar de plano

**Files:**
- Create: `web/lib/actions/assinatura.js`
- Create: `web/app/(app)/assinatura/page.js`
- Create: `web/components/EscolherPlanoForm.js`

**Interfaces:**
- Consumes: `PLANOS` de `@/lib/planos` (Task 3), `buscarUsuarioAtual` de `@/lib/data/usuario` (Task 4), `criarClienteAsaas`/`criarAssinaturaAsaas`/`buscarAssinaturaAsaas`/`atualizarAssinaturaAsaas`/`cancelarAssinaturaAsaas` de `@/lib/asaas` (Task 5).
- Produces: server action `escolherPlano(planoAlvo, prevState, formData)` — retorna `{ error }` ou `{ sucesso }`; componente `EscolherPlanoForm({ action })`.

- [ ] **Step 1: `web/lib/actions/assinatura.js`**

```js
"use server";

import { redirect } from "next/navigation";
import { revalidatePath } from "next/cache";
import { createClient } from "@/lib/supabase/server";
import { buscarUsuarioAtual } from "@/lib/data/usuario";
import { PLANOS } from "@/lib/planos";
import {
  criarClienteAsaas,
  criarAssinaturaAsaas,
  buscarAssinaturaAsaas,
  atualizarAssinaturaAsaas,
  cancelarAssinaturaAsaas,
} from "@/lib/asaas";

export async function escolherPlano(planoAlvo, prevState, formData) {
  const usuario = await buscarUsuarioAtual();
  const supabase = await createClient();

  if (planoAlvo === usuario.plano) {
    return { error: "Você já está nesse plano." };
  }

  const semAssinaturaAtiva = !usuario.asaas_subscription_id || usuario.assinatura_status === "cancelada";

  if (semAssinaturaAtiva) {
    if (planoAlvo === "gratis") {
      return { error: "Você já está no plano Grátis." };
    }

    let asaasCustomerId = usuario.asaas_customer_id;
    if (!asaasCustomerId) {
      const cliente = await criarClienteAsaas({ nome: usuario.nome, cpf: usuario.cpf });
      asaasCustomerId = cliente.id;
    }

    const assinatura = await criarAssinaturaAsaas({
      customerId: asaasCustomerId,
      value: PLANOS[planoAlvo].preco,
      description: `PsiAgente — ${PLANOS[planoAlvo].nome}`,
    });

    const { error } = await supabase
      .from("Usuarios")
      .update({
        asaas_customer_id: asaasCustomerId,
        asaas_subscription_id: assinatura.id,
        plano_pretendido: planoAlvo,
        plano_pretendido_a_partir_de: null,
      })
      .eq("id", usuario.id);

    if (error) {
      return { error: "Não foi possível iniciar a assinatura. Tente novamente." };
    }

    redirect(assinatura.invoiceUrl);
  }

  const assinaturaAtual = await buscarAssinaturaAsaas(usuario.asaas_subscription_id);

  if (planoAlvo === "gratis") {
    await cancelarAssinaturaAsaas(usuario.asaas_subscription_id);
  } else {
    await atualizarAssinaturaAsaas(usuario.asaas_subscription_id, { value: PLANOS[planoAlvo].preco });
  }

  const { error } = await supabase
    .from("Usuarios")
    .update({
      plano_pretendido: planoAlvo,
      plano_pretendido_a_partir_de: assinaturaAtual.nextDueDate,
    })
    .eq("id", usuario.id);

  if (error) {
    return { error: "Não foi possível agendar a troca de plano. Tente novamente." };
  }

  revalidatePath("/assinatura");
  const dataFormatada = new Date(`${assinaturaAtual.nextDueDate}T00:00:00`).toLocaleDateString("pt-BR");
  return { sucesso: `Sua mudança pra ${PLANOS[planoAlvo].nome} entra em vigor em ${dataFormatada}.` };
}
```

Nota: `usuario.plano_pago` não é usado como gatilho de "tem assinatura" porque ele fica preenchido mesmo depois de cair pra Grátis por inadimplência — o campo certo pra saber "existe uma assinatura ativa no Asaas agora" é `asaas_subscription_id` + `assinatura_status !== 'cancelada'`.

- [ ] **Step 2: `web/components/EscolherPlanoForm.js`**

```jsx
"use client";

import { useActionState } from "react";

const estadoInicial = {};

export default function EscolherPlanoForm({ action }) {
  const [state, formAction, pending] = useActionState(action, estadoInicial);

  return (
    <form action={formAction} className="space-y-2">
      {state?.error && <p className="text-sm text-red-600">{state.error}</p>}
      {state?.sucesso && <p className="text-sm text-green-700">{state.sucesso}</p>}
      <button type="submit" disabled={pending} className="btn-outline w-full disabled:opacity-50">
        {pending ? "Aguarde..." : "Escolher"}
      </button>
    </form>
  );
}
```

- [ ] **Step 3: `web/app/(app)/assinatura/page.js`**

```jsx
import { buscarUsuarioAtual } from "@/lib/data/usuario";
import { PLANOS } from "@/lib/planos";
import { escolherPlano } from "@/lib/actions/assinatura";
import EscolherPlanoForm from "@/components/EscolherPlanoForm";

export default async function PaginaAssinatura() {
  const usuario = await buscarUsuarioAtual();

  return (
    <div className="space-y-4">
      <h1 className="page-title">Assinatura</h1>

      {usuario.plano_pretendido && (
        <p className="text-sm text-blue-800 bg-blue-50 border border-blue-200 rounded-lg px-4 py-2">
          Sua mudança pra {PLANOS[usuario.plano_pretendido].nome} entra em vigor em{" "}
          {usuario.plano_pretendido_a_partir_de
            ? new Date(usuario.plano_pretendido_a_partir_de).toLocaleDateString("pt-BR")
            : "breve"}
          .
        </p>
      )}

      <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">
        {Object.values(PLANOS).map((plano) => (
          <div key={plano.id} className="card p-4 space-y-2">
            <p className="font-semibold text-navy">{plano.nome}</p>
            <p className="text-2xl font-bold text-navy">
              {plano.preco === 0 ? "Grátis" : `R$ ${plano.preco.toFixed(2).replace(".", ",")}`}
              {plano.preco > 0 && <span className="text-sm font-normal text-muted"> /mês</span>}
            </p>
            {usuario.plano === plano.id ? (
              <p className="text-sm text-muted">Plano atual</p>
            ) : (
              <EscolherPlanoForm action={escolherPlano.bind(null, plano.id)} />
            )}
          </div>
        ))}
      </div>
    </div>
  );
}
```

- [ ] **Step 4: Verificar que o build passa e a página renderiza**

```bash
cd web && npm run build
```

Esperado: `/assinatura` aparece na lista de rotas geradas (ver saída do build), sem erro de compilação.

- [ ] **Step 5: Commit**

```bash
git add web/lib/actions/assinatura.js web/app/\(app\)/assinatura/page.js web/components/EscolherPlanoForm.js
git commit -m "feat: pagina /assinatura para escolher e trocar de plano"
```

---

## Task 7: Webhook `POST /api/asaas/webhook`

**Files:**
- Create: `web/app/api/asaas/webhook/route.js`
- Modify: `web/lib/supabase/proxy.js:7-21` (adicionar `/api/asaas/webhook` a `PUBLIC_PATHS`)

**Interfaces:**
- Consumes: `createAdminClient` de `@/lib/supabase/admin`.
- Produces: rota `POST /api/asaas/webhook`, sem retorno consumido por outro código (endpoint terminal).

- [ ] **Step 1: Adicionar o path público**

Em `web/lib/supabase/proxy.js`, no array `PUBLIC_PATHS`, adicionar `"/api/asaas/webhook"` (mesma lista onde já está `"/carne-leao-automatico"`).

- [ ] **Step 2: Escrever a rota**

```js
// web/app/api/asaas/webhook/route.js
import { createAdminClient } from "@/lib/supabase/admin";

export async function POST(request) {
  const token = request.headers.get("asaas-access-token");
  if (!token || token !== process.env.ASAAS_WEBHOOK_TOKEN) {
    return new Response("Não autorizado.", { status: 401 });
  }

  const evento = await request.json();
  const admin = createAdminClient();

  const subscriptionId = evento.payment?.subscription ?? null;
  let usuario = null;
  if (subscriptionId) {
    const { data } = await admin
      .from("Usuarios")
      .select("id, plano_pago, plano_pretendido, plano_pretendido_a_partir_de")
      .eq("asaas_subscription_id", subscriptionId)
      .maybeSingle();
    usuario = data;
  }

  const { error: erroInsert } = await admin.from("EventoAssinatura").insert({
    usuario: usuario?.id ?? null,
    asaas_event_id: evento.id,
    tipo: evento.event,
    payload: evento,
  });

  if (erroInsert) {
    // Evento duplicado (unique constraint em asaas_event_id) -> ja
    // processado antes, ignora sem tratar como falha (entrega "pelo menos
    // uma vez" do Asaas pode repetir o mesmo evento).
    if (erroInsert.code === "23505") {
      return new Response("OK (evento duplicado, ignorado).", { status: 200 });
    }
    return new Response("Erro ao registrar evento.", { status: 500 });
  }

  if (!usuario) {
    return new Response("OK (assinatura não encontrada).", { status: 200 });
  }

  if (evento.event === "PAYMENT_CONFIRMED" || evento.event === "PAYMENT_RECEIVED") {
    const pendenciaLiberada =
      usuario.plano_pretendido &&
      (!usuario.plano_pretendido_a_partir_de || new Date(usuario.plano_pretendido_a_partir_de) <= new Date());

    const atualizacao = { assinatura_status: "ativa", assinatura_vencida_em: null };

    if (pendenciaLiberada) {
      atualizacao.plano = usuario.plano_pretendido;
      atualizacao.plano_pago = usuario.plano_pretendido;
      atualizacao.plano_pretendido = null;
      atualizacao.plano_pretendido_a_partir_de = null;
    } else if (usuario.plano_pago) {
      atualizacao.plano = usuario.plano_pago;
    }

    await admin.from("Usuarios").update(atualizacao).eq("id", usuario.id);
  } else if (evento.event === "PAYMENT_OVERDUE") {
    await admin
      .from("Usuarios")
      .update({ assinatura_status: "inadimplente", assinatura_vencida_em: new Date().toISOString() })
      .eq("id", usuario.id);
  }

  return new Response("OK", { status: 200 });
}
```

- [ ] **Step 3: Verificar — simula um payload de webhook direto contra a rota local**

Rodar o servidor local (`cd web && npm run build && npm run start`, ou `node .next/standalone/server.js` conforme o padrão já documentado pro projeto) e, em outro terminal:

```js
// scratch-verificar-webhook.js
const fs = require('fs');
function loadEnv(file) {
  const env = {};
  for (const line of fs.readFileSync(file, 'utf-8').split('\n')) {
    const m = line.match(/^([A-Z_]+)=(.*)$/);
    if (m) env[m[1]] = m[2].trim();
  }
  return env;
}
const env = loadEnv('web/.env.local');
const { createClient } = require('./web/node_modules/@supabase/supabase-js');

async function main() {
  const admin = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY);

  // Prepara um usuario de teste com uma assinatura "fake" pra casar com o payload
  const { data: usuarioTeste } = await admin
    .from('Usuarios')
    .update({ asaas_subscription_id: 'sub_teste_webhook', plano_pago: 'gestao', plano: 'gratis' })
    .eq('id', 20) // ajustar pro id do profissional de teste descartavel usado nesta sessao
    .select('id')
    .single();

  const payload = {
    id: 'evt_teste_' + Date.now(),
    event: 'PAYMENT_CONFIRMED',
    payment: { subscription: 'sub_teste_webhook' },
  };

  const resposta = await fetch('http://localhost:3000/api/asaas/webhook', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json', 'asaas-access-token': env.ASAAS_WEBHOOK_TOKEN },
    body: JSON.stringify(payload),
  });
  console.log('Status da resposta:', resposta.status);

  const { data: depois } = await admin.from('Usuarios').select('plano, assinatura_status').eq('id', usuarioTeste.id).single();
  console.log('Depois do webhook:', depois, '(esperado: plano=gestao, assinatura_status=ativa)');

  // limpa o estado de teste
  await admin.from('Usuarios').update({ asaas_subscription_id: null, plano_pago: null, plano: 'gratis', assinatura_status: null }).eq('id', usuarioTeste.id);
  await admin.from('EventoAssinatura').delete().eq('asaas_event_id', payload.id);
}
main().catch((e) => { console.error(e.message); process.exit(1); });
```

Esperado: status 200, `plano` do usuário de teste vira `gestao`, `assinatura_status` vira `ativa`. Apagar o script depois.

- [ ] **Step 4: Commit**

```bash
git add web/app/api/asaas/webhook/route.js web/lib/supabase/proxy.js
git commit -m "feat: webhook do Asaas confirma pagamento e marca inadimplencia"
```

---

## Task 8: Job diário `POST /api/assinaturas/aplicar-pendencias`

**Files:**
- Create: `web/app/api/assinaturas/aplicar-pendencias/route.js`
- Modify: `web/lib/supabase/proxy.js` (adicionar o novo path a `PUBLIC_PATHS`)

**Interfaces:**
- Consumes: `createAdminClient` de `@/lib/supabase/admin`.
- Produces: rota `POST /api/assinaturas/aplicar-pendencias`, retorna `{ downgrades: number, carencias: number }`.

- [ ] **Step 1: Adicionar env var nova**

```bash
cd web
cat >> .env.local << 'EOF'
ASSINATURA_CRON_SECRET=
EOF
```

Gerar um valor forte pra ela (ex.: `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`) e preencher — precisa ser o mesmo valor configurado depois no nó HTTP do n8n que vai chamar esse endpoint (fora do escopo desta task — é infra do n8n, mesma pendência já registrada pro item 9).

- [ ] **Step 2: Adicionar o path público**

Em `web/lib/supabase/proxy.js`, adicionar `"/api/assinaturas/aplicar-pendencias"` a `PUBLIC_PATHS`.

- [ ] **Step 3: Escrever a rota**

```js
// web/app/api/assinaturas/aplicar-pendencias/route.js
import { createAdminClient } from "@/lib/supabase/admin";

export async function POST(request) {
  const segredo = request.headers.get("x-cron-secret");
  if (!segredo || segredo !== process.env.ASSINATURA_CRON_SECRET) {
    return new Response("Não autorizado.", { status: 401 });
  }

  const admin = createAdminClient();
  const agora = new Date().toISOString();

  const { data: downgradesPendentes } = await admin
    .from("Usuarios")
    .select("id")
    .eq("plano_pretendido", "gratis")
    .lte("plano_pretendido_a_partir_de", agora);

  for (const usuario of downgradesPendentes ?? []) {
    await admin
      .from("Usuarios")
      .update({ plano: "gratis", plano_pago: null, plano_pretendido: null, plano_pretendido_a_partir_de: null })
      .eq("id", usuario.id);
    await admin.from("EventoAssinatura").insert({
      usuario: usuario.id,
      asaas_event_id: `downgrade-gratis-${usuario.id}-${agora}`,
      tipo: "downgrade_gratis_efetivado",
      payload: {},
    });
  }

  const cincoDiasAtras = new Date(Date.now() - 5 * 24 * 60 * 60 * 1000).toISOString();
  const { data: inadimplentes } = await admin
    .from("Usuarios")
    .select("id")
    .eq("assinatura_status", "inadimplente")
    .lte("assinatura_vencida_em", cincoDiasAtras)
    .neq("plano", "gratis");

  for (const usuario of inadimplentes ?? []) {
    await admin.from("Usuarios").update({ plano: "gratis" }).eq("id", usuario.id);
    await admin.from("EventoAssinatura").insert({
      usuario: usuario.id,
      asaas_event_id: `carencia-expirada-${usuario.id}-${agora}`,
      tipo: "carencia_expirada",
      payload: {},
    });
  }

  return Response.json({ downgrades: downgradesPendentes?.length ?? 0, carencias: inadimplentes?.length ?? 0 });
}
```

- [ ] **Step 4: Verificar contra produção com dado descartável**

```js
// scratch-verificar-aplicar-pendencias.js
const fs = require('fs');
function loadEnv(file) {
  const env = {};
  for (const line of fs.readFileSync(file, 'utf-8').split('\n')) {
    const m = line.match(/^([A-Z_]+)=(.*)$/);
    if (m) env[m[1]] = m[2].trim();
  }
  return env;
}
const env = loadEnv('web/.env.local');
const { createClient } = require('./web/node_modules/@supabase/supabase-js');

async function main() {
  const admin = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY);
  const idTeste = 20; // ajustar pro profissional de teste descartavel

  // cenario 1: downgrade pra gratis com data ja vencida
  await admin.from('Usuarios').update({
    plano: 'gestao_marketing', plano_pago: 'gestao_marketing',
    plano_pretendido: 'gratis', plano_pretendido_a_partir_de: '2020-01-01',
  }).eq('id', idTeste);

  const resposta = await fetch('http://localhost:3000/api/assinaturas/aplicar-pendencias', {
    method: 'POST',
    headers: { 'x-cron-secret': env.ASSINATURA_CRON_SECRET },
  });
  console.log('Status:', resposta.status, await resposta.json());

  const { data: depois } = await admin.from('Usuarios').select('plano, plano_pago, plano_pretendido').eq('id', idTeste).single();
  console.log('Depois:', depois, '(esperado: plano=gratis, plano_pago=null, plano_pretendido=null)');

  // limpa
  await admin.from('Usuarios').update({ plano: 'gratis', plano_pago: null, plano_pretendido: null, plano_pretendido_a_partir_de: null }).eq('id', idTeste);
  await admin.from('EventoAssinatura').delete().like('asaas_event_id', `downgrade-gratis-${idTeste}-%`);
}
main().catch((e) => { console.error(e.message); process.exit(1); });
```

Rodar com o servidor local no ar (mesmo `npm run start`/standalone da Task 7). Esperado conforme os comentários no script. Apagar o script depois.

- [ ] **Step 5: Commit**

```bash
git add web/app/api/assinaturas/aplicar-pendencias/route.js web/lib/supabase/proxy.js
git commit -m "feat: job diario aplica downgrades pendentes e expira carencia de inadimplencia"
```

---

## Task 9: Gates de recursos pagos (Recibos, Notas Fiscais, Carnê-Leão, WhatsApp)

**Files:**
- Create: `web/components/AvisoRecursoPago.js`
- Modify: `web/app/(app)/(gestao)/recibos/page.js`
- Modify: `web/lib/actions/recibos.js:6`
- Modify: `web/app/(app)/(gestao)/notas-fiscais/page.js`
- Modify: `web/lib/actions/notas-fiscais.js:8`
- Modify: `web/app/(app)/(gestao)/carne-leao/page.js`
- Modify: `web/app/(app)/(gestao)/carne-leao/gerar/route.js`
- Modify: `web/app/(app)/(gestao)/configuracoes/whatsapp/page.js`
- Modify: `web/lib/actions/whatsapp.js:6`

**Interfaces:**
- Consumes: `PLANOS` de `@/lib/planos` (Task 3).
- Produces: `<AvisoRecursoPago recurso="..." />` (componente server, sem estado).

- [ ] **Step 1: Componente de aviso**

```jsx
// web/components/AvisoRecursoPago.js
import Link from "next/link";

export default function AvisoRecursoPago({ recurso }) {
  return (
    <div className="card p-6 text-center space-y-3">
      <p className="text-navy font-semibold">{recurso} está disponível nos planos pagos.</p>
      <Link href="/assinatura" className="btn-primary inline-block">
        Ver planos
      </Link>
    </div>
  );
}
```

- [ ] **Step 2: Gate em `recibos/page.js`**

No topo do componente, logo após buscar `usuario` (ou adicionar a busca se a página ainda não busca `usuario` — ela hoje não busca, então adicionar):

```jsx
import { listarSessoesElegiveisParaRecibo, listarRecibosEmitidos } from "@/lib/data/recibos";
import { gerarRecibo } from "@/lib/actions/recibos";
import { buscarUsuarioAtual } from "@/lib/data/usuario";
import { PLANOS } from "@/lib/planos";
import AvisoRecursoPago from "@/components/AvisoRecursoPago";

export default async function PaginaRecibos() {
  const usuario = await buscarUsuarioAtual();
  if (!PLANOS[usuario.plano].temDocumentos) {
    return <AvisoRecursoPago recurso="A emissão de recibos" />;
  }

  const [elegiveis, emitidos] = await Promise.all([listarSessoesElegiveisParaRecibo(), listarRecibosEmitidos()]);
  // ... resto do componente sem mudanca
```

- [ ] **Step 3: Enforcement real em `gerarRecibo`**

```js
// web/lib/actions/recibos.js
"use server";

import { revalidatePath } from "next/cache";
import { createClient } from "@/lib/supabase/server";
import { buscarUsuarioAtual } from "@/lib/data/usuario";
import { PLANOS } from "@/lib/planos";

export async function gerarRecibo(sessaoId) {
  const usuario = await buscarUsuarioAtual();
  if (!PLANOS[usuario.plano].temDocumentos) return;

  const supabase = await createClient();
  // ... resto da funcao sem mudanca
```

- [ ] **Step 4: Mesmo padrão em Notas Fiscais**

Em `web/app/(app)/(gestao)/notas-fiscais/page.js`: adicionar a mesma checagem `!PLANOS[usuario.plano].temDocumentos` no topo (a página já busca `usuario` via `buscarUsuarioAtual` — confirmar lendo o arquivo antes de editar; se não buscar, adicionar igual ao Step 2).

Em `web/lib/actions/notas-fiscais.js`, no início de `emitirNotaFiscal(pagamentoId, prevState, formData)`:

```js
export async function emitirNotaFiscal(pagamentoId, prevState, formData) {
  const usuario = await buscarUsuarioAtual();
  if (!PLANOS[usuario.plano].temDocumentos) {
    return { error: "Emissão de Nota Fiscal disponível apenas nos planos pagos." };
  }
  // ... resto da funcao sem mudanca
```

(adicionar os imports de `buscarUsuarioAtual` e `PLANOS` no topo do arquivo.)

- [ ] **Step 5: Mesmo padrão em Carnê-Leão**

Em `web/app/(app)/(gestao)/carne-leao/page.js`, logo após buscar `usuario` (a página já faz isso — `buscarUsuarioAtual()` na linha do `Promise.all`):

```jsx
  const [usuario, { elegiveis, semCpf }] = await Promise.all([
    buscarUsuarioAtual(),
    listarPagamentosElegiveis({ dataInicio: inicio, dataFim: fim }),
  ]);

  if (!PLANOS[usuario.plano].temCarneLeao) {
    return <AvisoRecursoPago recurso="O Carnê-Leão" />;
  }
```

(adicionar imports de `PLANOS` e `AvisoRecursoPago`.)

Em `web/app/(app)/(gestao)/carne-leao/gerar/route.js`, logo após buscar `usuario`:

```js
  const usuario = await buscarUsuarioAtual();
  if (!PLANOS[usuario.plano].temCarneLeao) {
    return new Response("Recurso disponível apenas nos planos pagos.", { status: 403 });
  }
  if (!cpfValido(usuario.cpf)) {
    // ... resto sem mudanca
```

(adicionar import de `PLANOS`.)

- [ ] **Step 6: Mesmo padrão no WhatsApp**

Em `web/app/(app)/(gestao)/configuracoes/whatsapp/page.js`: adicionar a checagem `!PLANOS[usuario.plano].temWhatsapp` (confirmar se a página já busca `usuario`; se não, adicionar `buscarUsuarioAtual()`).

Em `web/lib/actions/whatsapp.js`, início de `vincularWhatsapp`:

```js
export async function vincularWhatsapp(prevState, formData) {
  const usuario = await buscarUsuarioAtual();
  if (!PLANOS[usuario.plano].temWhatsapp) {
    return { error: "Agente de WhatsApp disponível apenas nos planos pagos." };
  }
  // ... resto sem mudanca
```

(adicionar imports.)

- [ ] **Step 7: Verificar — build + checagem funcional via script**

```bash
cd web && npm run build
```

Depois, verificar a lógica de bloqueio com um script (sem precisar de navegador, já que estamos chamando as Server Actions indiretamente via a mesma checagem que elas fazem):

```js
// scratch-verificar-gates.js
const fs = require('fs');
function loadEnv(file) {
  const env = {};
  for (const line of fs.readFileSync(file, 'utf-8').split('\n')) {
    const m = line.match(/^([A-Z_]+)=(.*)$/);
    if (m) env[m[1]] = m[2].trim();
  }
  return env;
}
const env = loadEnv('web/.env.local');
const { createClient } = require('./web/node_modules/@supabase/supabase-js');

async function main() {
  const admin = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY);
  const idTeste = 20; // profissional de teste descartavel

  await admin.from('Usuarios').update({ plano: 'gratis' }).eq('id', idTeste);
  const { data: usuarioGratis } = await admin.from('Usuarios').select('plano').eq('id', idTeste).single();
  const { PLANOS } = await import('./web/lib/planos.js');
  console.log('Grátis tem documentos?', PLANOS[usuarioGratis.plano].temDocumentos, '(esperado: false)');
  console.log('Grátis tem WhatsApp?', PLANOS[usuarioGratis.plano].temWhatsapp, '(esperado: false)');
  console.log('Grátis tem Carnê-Leão?', PLANOS[usuarioGratis.plano].temCarneLeao, '(esperado: false)');

  await admin.from('Usuarios').update({ plano: 'gestao_marketing' }).eq('id', idTeste);
  console.log('gestao_marketing tem documentos?', PLANOS.gestao_marketing.temDocumentos, '(esperado: true)');

  // restaura
  await admin.from('Usuarios').update({ plano: 'gratis' }).eq('id', idTeste);
}
main();
```

Esperado: conforme os comentários. Isso valida a lógica de `PLANOS[...]` usada nos gates (a parte que dá pra testar sem navegador — o clique real na UI fica pendente do mesmo bug de cookie em Server Action já registrado, ver Task 14). Apagar o script depois.

- [ ] **Step 8: Commit**

```bash
git add web/components/AvisoRecursoPago.js \
  "web/app/(app)/(gestao)/recibos/page.js" web/lib/actions/recibos.js \
  "web/app/(app)/(gestao)/notas-fiscais/page.js" web/lib/actions/notas-fiscais.js \
  "web/app/(app)/(gestao)/carne-leao/page.js" "web/app/(app)/(gestao)/carne-leao/gerar/route.js" \
  "web/app/(app)/(gestao)/configuracoes/whatsapp/page.js" web/lib/actions/whatsapp.js
git commit -m "feat: bloqueia documentos, carne-leao e whatsapp no plano Gratis"
```

---

## Task 10: Trava de 1 consultório no plano Grátis

**Files:**
- Modify: `web/lib/actions/consultorios.js:7-23`
- Modify: `web/components/ConsultorioForm.js`

**Interfaces:**
- Consumes: `PLANOS` de `@/lib/planos`, `buscarUsuarioAtual`.
- Produces: `criarConsultorio` pode retornar `{ error, bloqueadoPorPlano: true }`.

- [ ] **Step 1: Editar `criarConsultorio`**

```js
"use server";

import { redirect } from "next/navigation";
import { revalidatePath } from "next/cache";
import { createClient } from "@/lib/supabase/server";
import { buscarUsuarioAtual } from "@/lib/data/usuario";
import { PLANOS } from "@/lib/planos";

export async function criarConsultorio(prevState, formData) {
  const usuario = await buscarUsuarioAtual();
  const supabase = await createClient();

  const limite = PLANOS[usuario.plano].limiteConsultorios;
  if (limite !== null) {
    const { count } = await supabase.from("Consultorio").select("id", { count: "exact", head: true });
    if ((count ?? 0) >= limite) {
      return {
        error: `Seu plano permite até ${limite} consultório(s). Faça upgrade para adicionar mais.`,
        bloqueadoPorPlano: true,
      };
    }
  }

  const { error } = await supabase.from("Consultorio").insert({
    nome: formData.get("nome"),
    endereco: formData.get("endereco") || null,
    telefone: formData.get("telefone"),
    email_atendimento: formData.get("email_atendimento"),
  });

  if (error) {
    return { error: "Não foi possível salvar o consultório." };
  }

  revalidatePath("/consultorios");
  redirect("/consultorios");
}
```

(`atualizarConsultorio` não muda — o limite só se aplica à criação.)

- [ ] **Step 2: Mostrar link de upgrade em `ConsultorioForm.js`**

Substituir a linha `{state?.error && <p className="text-sm text-red-600">{state.error}</p>}` por:

```jsx
      {state?.error && (
        <div className="text-sm text-red-600 space-y-1">
          <p>{state.error}</p>
          {state.bloqueadoPorPlano && (
            <Link href="/assinatura" className="link font-semibold">
              Ver planos
            </Link>
          )}
        </div>
      )}
```

(adicionar `import Link from "next/link";` no topo do arquivo.)

- [ ] **Step 3: Verificar contra produção**

```js
// scratch-verificar-limite-consultorio.js
const fs = require('fs');
function loadEnv(file) {
  const env = {};
  for (const line of fs.readFileSync(file, 'utf-8').split('\n')) {
    const m = line.match(/^([A-Z_]+)=(.*)$/);
    if (m) env[m[1]] = m[2].trim();
  }
  return env;
}
const env = loadEnv('web/.env.local');
const { createClient } = require('./web/node_modules/@supabase/supabase-js');

async function main() {
  const admin = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY);
  const idTeste = 20;
  const { data: usuarioTeste } = await admin.from('Usuarios').select('id_user').eq('id', idTeste).single();

  await admin.from('Usuarios').update({ plano: 'gratis' }).eq('id', idTeste);

  const { count } = await admin.from('Consultorio').select('id', { count: 'exact', head: true }).eq('owner', usuarioTeste.id_user);
  console.log('Consultórios do profissional de teste hoje:', count);

  const { PLANOS } = await import('./web/lib/planos.js');
  const bloqueado = count >= PLANOS.gratis.limiteConsultorios;
  console.log('Bloquearia criação de novo consultório no Grátis?', bloqueado, '(esperado: true se já tem >= 1)');

  await admin.from('Usuarios').update({ plano: 'gratis' }).eq('id', idTeste); // deixa como estava
}
main();
```

Esperado: reflete corretamente se o profissional de teste (que já tem pelo menos 1 consultório, usado nos testes do item 10 anteriores) seria bloqueado. Apagar o script depois.

- [ ] **Step 4: Commit**

```bash
git add web/lib/actions/consultorios.js web/components/ConsultorioForm.js
git commit -m "feat: limita criacao de consultorio a 1 no plano Gratis"
```

---

## Task 11: `SidebarNav` — indicador visual nos itens bloqueados

**Files:**
- Modify: `web/components/SidebarNav.js`

**Interfaces:**
- Nenhuma nova — só muda a renderização condicional a partir da prop `plano` já recebida.

- [ ] **Step 1: Marcar os itens bloqueados no array `ITENS_NAV`**

```js
    itens: [
      { href: "/recibos", label: "Recibos", Icone: IconeRecibo, pro: true },
      { href: "/notas-fiscais", label: "Notas Fiscais", Icone: IconeNotaFiscal, pro: true },
      { href: "/carne-leao", label: "Carnê-Leão", Icone: IconeCarneLeao, pro: true },
    ],
```

E, no grupo `configuracoes`:

```js
      { href: "/configuracoes/whatsapp", label: "WhatsApp", Icone: IconeWhatsapp, pro: true },
```

- [ ] **Step 2: Renderizar o selo "PRO" em `ItemNav`**

```jsx
  function ItemNav({ item, compacta, onNavegar }) {
    const { href, label, Icone, exact, pro } = item;
    const ativo = estaAtivo(href, exact);
    return (
      <Link
        href={href}
        onClick={onNavegar}
        title={compacta ? label : undefined}
        className={`flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm font-semibold text-navy transition-colors hover:bg-background ${
          ativo ? "bg-background" : ""
        } ${compacta ? "justify-center" : ""}`}
      >
        <Icone className={ativo ? "shrink-0 text-primary" : "shrink-0 text-muted"} />
        {!compacta && (
          <span className="flex-1 flex items-center justify-between">
            {label}
            {pro && plano === "gratis" && (
              <span className="text-[10px] font-bold text-primary bg-primary/10 rounded px-1.5 py-0.5">PRO</span>
            )}
          </span>
        )}
      </Link>
    );
  }
```

(`ItemNav` já está definido dentro do componente `SidebarNav`, então `plano` já está no escopo — não precisa de prop nova.)

- [ ] **Step 3: Verificar visualmente**

```bash
cd web && npm run build && npm run start
```

Abrir `http://localhost:3000` logado com um usuário no plano Grátis (usar o profissional de teste, setando `plano = 'gratis'` via o admin client como nas tasks anteriores) e conferir visualmente que "Recibos", "Notas Fiscais", "Carnê-Leão" e "WhatsApp" mostram o selo "PRO" na sidebar. Depois, mudar o `plano` do mesmo usuário pra `gestao_marketing` e conferir que o selo some.

- [ ] **Step 4: Commit**

```bash
git add web/components/SidebarNav.js
git commit -m "feat: selo PRO nos itens bloqueados da sidebar pro plano Gratis"
```

---

## Task 12: Migration — RPCs do agente de WhatsApp trocam consultório por owner

**Files:**
- Create: `supabase/migrations/20260827000002_agent_rpc_remove_consultorio_scope.sql`

**Interfaces:**
- Produces: mesmas 15 RPCs (`agent_agendar_sessao_avulsa`, `agent_buscar_paciente`, `agent_cancelar_sessao`, `agent_excluir_pagamento`, `agent_excluir_sessao`, `agent_gerar_recibo`, `agent_get_agenda`, `agent_listar_debitos_paciente`, `agent_listar_inadimplentes`, `agent_marcar_atendimento_realizado`, `agent_reagendar_sessao`, `agent_registrar_anamnese`, `agent_registrar_lancamento_despesa`, `agent_registrar_pagamento_sessao`, `agent_resumo_financeiro`) com a **mesma assinatura** (nenhum chamador externo precisa mudar), mas escopando por `owner` em vez de `consultorio`. Remove `_agent_resolve_consultorio`, `agent_listar_consultorios`, `agent_definir_consultorio_ativo` e a coluna `agent_sessions.consultorio_ativo_id`.

- [ ] **Step 1: Escrever a migration completa**

```sql
-- supabase/migrations/20260827000002_agent_rpc_remove_consultorio_scope.sql
-- Remove o escopo por "consultorio ativo da conversa" de todas as RPCs do
-- agente de WhatsApp. `owner` (ja existente em Paciente/Sessao/etc, usado
-- pelo RLS) sempre foi a barreira de seguranca real -- o filtro por
-- consultorio nunca foi isso, so um recorte de UX. Mesma assinatura em
-- toda funcao (mantem p_consultorio_id no parametro, so nao usa mais no
-- corpo) para nao precisar de DROP FUNCTION + regrant.

CREATE OR REPLACE FUNCTION public.agent_agendar_sessao_avulsa(p_whatsapp_number text, p_paciente_id bigint, p_data date, p_horario time without time zone, p_duracao_min numeric DEFAULT 50, p_consultorio_id bigint DEFAULT NULL::bigint)
 RETURNS bigint
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'public'
AS $function$
declare
  v_owner uuid;
  v_paciente_ok bigint;
  v_sessao_id bigint;
begin
  v_owner := public._agent_get_owner_uuid(p_whatsapp_number);

  select id into v_paciente_ok
  from "Paciente"
  where id = p_paciente_id and owner = v_owner;

  if v_paciente_ok is null then
    raise exception 'PACIENTE_INVALIDO' using errcode = 'P0001';
  end if;

  insert into "Sessao" (paciente, data, horario, duracao_min, tipo_sessao, status, owner, "Realizado")
  values (p_paciente_id, p_data, p_horario, p_duracao_min, 'avulso', 'marcada', v_owner, false)
  returning id into v_sessao_id;

  return v_sessao_id;
end;
$function$;

CREATE OR REPLACE FUNCTION public.agent_buscar_paciente(p_whatsapp_number text, p_nome text, p_consultorio_id bigint DEFAULT NULL::bigint)
 RETURNS TABLE(id bigint, nome text, similaridade real)
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'public'
AS $function$
declare
  v_owner uuid;
begin
  v_owner := public._agent_get_owner_uuid(p_whatsapp_number);

  return query
  select p.id, p.nome, similarity(p.nome, p_nome) as similaridade
  from "Paciente" p
  where p.owner = v_owner
    and similarity(p.nome, p_nome) > 0.3
  order by similaridade desc
  limit 5;
end;
$function$;

CREATE OR REPLACE FUNCTION public.agent_cancelar_sessao(p_whatsapp_number text, p_sessao_id bigint, p_consultorio_id bigint DEFAULT NULL::bigint)
 RETURNS boolean
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'public'
AS $function$
declare
  v_owner uuid;
begin
  v_owner := public._agent_get_owner_uuid(p_whatsapp_number);

  update "Sessao" s
  set status = 'cancelada'
  where s.id = p_sessao_id
    and s.owner = v_owner;

  if not found then
    raise exception 'SESSAO_NAO_ENCONTRADA' using errcode = 'P0001';
  end if;

  return true;
end;
$function$;

CREATE OR REPLACE FUNCTION public.agent_excluir_pagamento(p_whatsapp_number text, p_pagamento_id bigint, p_consultorio_id bigint DEFAULT NULL::bigint)
 RETURNS boolean
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'public'
AS $function$
declare
  v_owner uuid;
  v_pagamento_id bigint;
  v_lancamento_id bigint;
begin
  v_owner := public._agent_get_owner_uuid(p_whatsapp_number);

  select pg.id, pg.lancamento into v_pagamento_id, v_lancamento_id
  from "PagamentoSessao" pg
  join "Sessao" s on s.id = pg.sessao
  where pg.id = p_pagamento_id and s.owner = v_owner;

  if v_pagamento_id is null then
    raise exception 'PAGAMENTO_NAO_ENCONTRADO' using errcode = 'P0001';
  end if;

  delete from "PagamentoSessao" where id = v_pagamento_id;

  if v_lancamento_id is not null then
    delete from "LancamentoFinanceiro" where id = v_lancamento_id;
  end if;

  return true;
exception
  when foreign_key_violation then
    raise exception 'PAGAMENTO_TEM_NOTA_FISCAL' using errcode = 'P0001';
end;
$function$;

CREATE OR REPLACE FUNCTION public.agent_excluir_sessao(p_whatsapp_number text, p_sessao_id bigint, p_consultorio_id bigint DEFAULT NULL::bigint)
 RETURNS boolean
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'public'
AS $function$
declare
  v_owner uuid;
begin
  v_owner := public._agent_get_owner_uuid(p_whatsapp_number);

  delete from "Sessao" s
  where s.id = p_sessao_id
    and s.owner = v_owner;

  if not found then
    raise exception 'SESSAO_NAO_ENCONTRADA' using errcode = 'P0001';
  end if;

  return true;
exception
  when foreign_key_violation then
    raise exception 'SESSAO_TEM_VINCULO_FINANCEIRO' using errcode = 'P0001';
end;
$function$;

CREATE OR REPLACE FUNCTION public.agent_gerar_recibo(p_whatsapp_number text, p_sessao_id bigint, p_consultorio_id bigint DEFAULT NULL::bigint)
 RETURNS bigint
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'public'
AS $function$
declare
  v_owner uuid;
  v_paciente_id bigint;
  v_consultorio_id bigint;
  v_recibo_id bigint;
begin
  v_owner := public._agent_get_owner_uuid(p_whatsapp_number);

  select s.paciente, p.consultorio into v_paciente_id, v_consultorio_id
  from "Sessao" s
  join "Paciente" p on p.id = s.paciente
  where s.id = p_sessao_id and s.owner = v_owner;

  if v_paciente_id is null then
    raise exception 'SESSAO_NAO_ENCONTRADA' using errcode = 'P0001';
  end if;

  insert into "Recibo" (sessao, consultorio, paciente, data_emissao, owner)
  values (p_sessao_id, v_consultorio_id, v_paciente_id, current_date, v_owner)
  returning id into v_recibo_id;

  return v_recibo_id;
end;
$function$;

CREATE OR REPLACE FUNCTION public.agent_get_agenda(p_whatsapp_number text, p_data_inicio date, p_data_fim date, p_consultorio_id bigint DEFAULT NULL::bigint)
 RETURNS TABLE(sessao_id bigint, paciente_nome text, data date, horario time without time zone, duracao_min numeric, status text, realizado boolean)
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'public'
AS $function$
declare
  v_owner uuid;
begin
  v_owner := public._agent_get_owner_uuid(p_whatsapp_number);

  return query
  select s.id, p.nome, s.data, s.horario, s.duracao_min, s.status, s."Realizado"
  from "Sessao" s
  join "Paciente" p on p.id = s.paciente
  where s.owner = v_owner
    and s.data between p_data_inicio and p_data_fim
  order by s.data, s.horario;
end;
$function$;

CREATE OR REPLACE FUNCTION public.agent_listar_debitos_paciente(p_whatsapp_number text, p_paciente_id bigint, p_consultorio_id bigint DEFAULT NULL::bigint)
 RETURNS TABLE(sessao_id bigint, data date, valor_devido real)
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'public'
AS $function$
declare
  v_owner uuid;
begin
  v_owner := public._agent_get_owner_uuid(p_whatsapp_number);

  return query
  select s.id, s.data, coalesce(pac.valor_sessao, 0)
  from "Sessao" s
  join "Paciente" pac on pac.id = s.paciente
  left join "PagamentoSessao" pg on pg.sessao = s.id
  where s.owner = v_owner
    and s.paciente = p_paciente_id
    and s."Realizado" = true
    and pg.id is null
  order by s.data;
end;
$function$;

CREATE OR REPLACE FUNCTION public.agent_listar_inadimplentes(p_whatsapp_number text, p_consultorio_id bigint DEFAULT NULL::bigint)
 RETURNS TABLE(paciente_id bigint, paciente_nome text, sessao_id bigint, data date, valor_devido real)
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'public'
AS $function$
declare
  v_owner uuid;
begin
  v_owner := public._agent_get_owner_uuid(p_whatsapp_number);

  return query
  select p.id, p.nome, s.id, s.data, coalesce(p.valor_sessao, 0)
  from "Sessao" s
  join "Paciente" p on p.id = s.paciente
  left join "PagamentoSessao" pg on pg.sessao = s.id
  where s.owner = v_owner
    and s."Realizado" = true
    and pg.id is null
  order by s.data;
end;
$function$;

CREATE OR REPLACE FUNCTION public.agent_marcar_atendimento_realizado(p_whatsapp_number text, p_sessao_id bigint, p_anotacoes text DEFAULT NULL::text, p_consultorio_id bigint DEFAULT NULL::bigint)
 RETURNS boolean
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'public'
AS $function$
declare
  v_owner uuid;
begin
  v_owner := public._agent_get_owner_uuid(p_whatsapp_number);

  update "Sessao" s
  set status = 'realizada',
      "Realizado" = true,
      anotacoes = coalesce(p_anotacoes, s.anotacoes)
  where s.id = p_sessao_id
    and s.owner = v_owner;

  if not found then
    raise exception 'SESSAO_NAO_ENCONTRADA' using errcode = 'P0001';
  end if;

  return true;
end;
$function$;

CREATE OR REPLACE FUNCTION public.agent_reagendar_sessao(p_whatsapp_number text, p_sessao_id bigint, p_data_nova date, p_horario_novo time without time zone, p_consultorio_id bigint DEFAULT NULL::bigint)
 RETURNS jsonb
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'public'
AS $function$
declare
  v_owner uuid;
  v_paciente_id bigint;
  v_data_anterior date;
  v_horario_anterior time;
  v_reagendamentos_mes int;
  v_inicio_mes timestamptz;
begin
  v_owner := public._agent_get_owner_uuid(p_whatsapp_number);

  select s.paciente, s.data, s.horario
    into v_paciente_id, v_data_anterior, v_horario_anterior
  from "Sessao" s
  where s.id = p_sessao_id
    and s.owner = v_owner
    and coalesce(s."Realizado", false) = false
    and s.status is distinct from 'Cancelada';

  if v_paciente_id is null then
    raise exception 'SESSAO_NAO_REAGENDAVEL' using errcode = 'P0001';
  end if;

  update "Sessao"
  set data = p_data_nova, horario = p_horario_novo
  where id = p_sessao_id;

  insert into "SessaoReagendamento" (sessao, paciente, data_anterior, horario_anterior, data_nova, horario_novo)
  values (p_sessao_id, v_paciente_id, v_data_anterior, v_horario_anterior, p_data_nova, p_horario_novo);

  v_inicio_mes := date_trunc('month', now() at time zone 'America/Sao_Paulo') at time zone 'America/Sao_Paulo';

  select count(*) into v_reagendamentos_mes
  from "SessaoReagendamento"
  where paciente = v_paciente_id
    and reagendado_em >= v_inicio_mes
    and reagendado_em < v_inicio_mes + interval '1 month';

  return jsonb_build_object(
    'sessao_id', p_sessao_id,
    'reagendamentos_mes_atual', v_reagendamentos_mes,
    'alerta', v_reagendamentos_mes >= 3
  );
end;
$function$;

CREATE OR REPLACE FUNCTION public.agent_registrar_anamnese(p_whatsapp_number text, p_paciente_id bigint, p_campos jsonb DEFAULT '{}'::jsonb, p_observacao text DEFAULT NULL::text, p_consultorio_id bigint DEFAULT NULL::bigint)
 RETURNS jsonb
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'public'
AS $function$
declare
  v_owner uuid;
  v_paciente_ok bigint;
  v_atual "Anamnese";
  v_anamnese_id bigint;
  v_alteracoes jsonb := '[]'::jsonb;
  v_chave text;
  v_campos_validos text[] := array[
    'medicacao_em_uso','medico_responsavel','terapia_desde','atendido_desde',
    'queixa_inicial','desenvolvimento_queixa','historico_familiar',
    'tratamento_anterior','uso_substancias','hipotese_diagnostica','expectativas'
  ];
begin
  p_campos := coalesce(p_campos, '{}'::jsonb);
  if jsonb_typeof(p_campos) <> 'object' then
    raise exception 'CAMPOS_INVALIDOS' using errcode = 'P0001';
  end if;

  v_owner := public._agent_get_owner_uuid(p_whatsapp_number);

  select id into v_paciente_ok
  from "Paciente"
  where id = p_paciente_id and owner = v_owner;

  if v_paciente_ok is null then
    raise exception 'PACIENTE_INVALIDO' using errcode = 'P0001';
  end if;

  for v_chave in select jsonb_object_keys(p_campos) loop
    if not (v_chave = any(v_campos_validos)) then
      raise exception 'CAMPO_ANAMNESE_INVALIDO' using errcode = 'P0001';
    end if;
  end loop;

  select * into v_atual from "Anamnese" where paciente = p_paciente_id;

  if p_campos ? 'medicacao_em_uso' and v_atual.medicacao_em_uso is distinct from nullif(trim(p_campos->>'medicacao_em_uso'), '') then
    v_alteracoes := v_alteracoes || jsonb_build_object('campo', 'medicacao_em_uso', 'valor_anterior', v_atual.medicacao_em_uso, 'valor_novo', nullif(trim(p_campos->>'medicacao_em_uso'), ''));
  end if;
  if p_campos ? 'medico_responsavel' and v_atual.medico_responsavel is distinct from nullif(trim(p_campos->>'medico_responsavel'), '') then
    v_alteracoes := v_alteracoes || jsonb_build_object('campo', 'medico_responsavel', 'valor_anterior', v_atual.medico_responsavel, 'valor_novo', nullif(trim(p_campos->>'medico_responsavel'), ''));
  end if;
  if p_campos ? 'terapia_desde' and v_atual.terapia_desde is distinct from nullif(trim(p_campos->>'terapia_desde'), '') then
    v_alteracoes := v_alteracoes || jsonb_build_object('campo', 'terapia_desde', 'valor_anterior', v_atual.terapia_desde, 'valor_novo', nullif(trim(p_campos->>'terapia_desde'), ''));
  end if;
  if p_campos ? 'atendido_desde' and v_atual.atendido_desde is distinct from nullif(trim(p_campos->>'atendido_desde'), '') then
    v_alteracoes := v_alteracoes || jsonb_build_object('campo', 'atendido_desde', 'valor_anterior', v_atual.atendido_desde, 'valor_novo', nullif(trim(p_campos->>'atendido_desde'), ''));
  end if;
  if p_campos ? 'queixa_inicial' and v_atual.queixa_inicial is distinct from nullif(trim(p_campos->>'queixa_inicial'), '') then
    v_alteracoes := v_alteracoes || jsonb_build_object('campo', 'queixa_inicial', 'valor_anterior', v_atual.queixa_inicial, 'valor_novo', nullif(trim(p_campos->>'queixa_inicial'), ''));
  end if;
  if p_campos ? 'desenvolvimento_queixa' and v_atual.desenvolvimento_queixa is distinct from nullif(trim(p_campos->>'desenvolvimento_queixa'), '') then
    v_alteracoes := v_alteracoes || jsonb_build_object('campo', 'desenvolvimento_queixa', 'valor_anterior', v_atual.desenvolvimento_queixa, 'valor_novo', nullif(trim(p_campos->>'desenvolvimento_queixa'), ''));
  end if;
  if p_campos ? 'historico_familiar' and v_atual.historico_familiar is distinct from nullif(trim(p_campos->>'historico_familiar'), '') then
    v_alteracoes := v_alteracoes || jsonb_build_object('campo', 'historico_familiar', 'valor_anterior', v_atual.historico_familiar, 'valor_novo', nullif(trim(p_campos->>'historico_familiar'), ''));
  end if;
  if p_campos ? 'tratamento_anterior' and v_atual.tratamento_anterior is distinct from nullif(trim(p_campos->>'tratamento_anterior'), '') then
    v_alteracoes := v_alteracoes || jsonb_build_object('campo', 'tratamento_anterior', 'valor_anterior', v_atual.tratamento_anterior, 'valor_novo', nullif(trim(p_campos->>'tratamento_anterior'), ''));
  end if;
  if p_campos ? 'uso_substancias' and v_atual.uso_substancias is distinct from nullif(trim(p_campos->>'uso_substancias'), '') then
    v_alteracoes := v_alteracoes || jsonb_build_object('campo', 'uso_substancias', 'valor_anterior', v_atual.uso_substancias, 'valor_novo', nullif(trim(p_campos->>'uso_substancias'), ''));
  end if;
  if p_campos ? 'hipotese_diagnostica' and v_atual.hipotese_diagnostica is distinct from nullif(trim(p_campos->>'hipotese_diagnostica'), '') then
    v_alteracoes := v_alteracoes || jsonb_build_object('campo', 'hipotese_diagnostica', 'valor_anterior', v_atual.hipotese_diagnostica, 'valor_novo', nullif(trim(p_campos->>'hipotese_diagnostica'), ''));
  end if;
  if p_campos ? 'expectativas' and v_atual.expectativas is distinct from nullif(trim(p_campos->>'expectativas'), '') then
    v_alteracoes := v_alteracoes || jsonb_build_object('campo', 'expectativas', 'valor_anterior', v_atual.expectativas, 'valor_novo', nullif(trim(p_campos->>'expectativas'), ''));
  end if;

  if v_atual.id is null and jsonb_array_length(v_alteracoes) = 0 and p_observacao is null then
    return jsonb_build_object('anamnese_id', null, 'alteracoes', '[]'::jsonb);
  end if;

  insert into "Anamnese" (
    paciente, medicacao_em_uso, medico_responsavel, terapia_desde, atendido_desde,
    queixa_inicial, desenvolvimento_queixa, historico_familiar, tratamento_anterior,
    uso_substancias, hipotese_diagnostica, expectativas, atualizado_em
  )
  values (
    p_paciente_id,
    case when p_campos ? 'medicacao_em_uso' then nullif(trim(p_campos->>'medicacao_em_uso'), '') else v_atual.medicacao_em_uso end,
    case when p_campos ? 'medico_responsavel' then nullif(trim(p_campos->>'medico_responsavel'), '') else v_atual.medico_responsavel end,
    case when p_campos ? 'terapia_desde' then nullif(trim(p_campos->>'terapia_desde'), '') else v_atual.terapia_desde end,
    case when p_campos ? 'atendido_desde' then nullif(trim(p_campos->>'atendido_desde'), '') else v_atual.atendido_desde end,
    case when p_campos ? 'queixa_inicial' then nullif(trim(p_campos->>'queixa_inicial'), '') else v_atual.queixa_inicial end,
    case when p_campos ? 'desenvolvimento_queixa' then nullif(trim(p_campos->>'desenvolvimento_queixa'), '') else v_atual.desenvolvimento_queixa end,
    case when p_campos ? 'historico_familiar' then nullif(trim(p_campos->>'historico_familiar'), '') else v_atual.historico_familiar end,
    case when p_campos ? 'tratamento_anterior' then nullif(trim(p_campos->>'tratamento_anterior'), '') else v_atual.tratamento_anterior end,
    case when p_campos ? 'uso_substancias' then nullif(trim(p_campos->>'uso_substancias'), '') else v_atual.uso_substancias end,
    case when p_campos ? 'hipotese_diagnostica' then nullif(trim(p_campos->>'hipotese_diagnostica'), '') else v_atual.hipotese_diagnostica end,
    case when p_campos ? 'expectativas' then nullif(trim(p_campos->>'expectativas'), '') else v_atual.expectativas end,
    now()
  )
  on conflict (paciente) do update set
    medicacao_em_uso = excluded.medicacao_em_uso,
    medico_responsavel = excluded.medico_responsavel,
    terapia_desde = excluded.terapia_desde,
    atendido_desde = excluded.atendido_desde,
    queixa_inicial = excluded.queixa_inicial,
    desenvolvimento_queixa = excluded.desenvolvimento_queixa,
    historico_familiar = excluded.historico_familiar,
    tratamento_anterior = excluded.tratamento_anterior,
    uso_substancias = excluded.uso_substancias,
    hipotese_diagnostica = excluded.hipotese_diagnostica,
    expectativas = excluded.expectativas,
    atualizado_em = excluded.atualizado_em
  returning id into v_anamnese_id;

  if jsonb_array_length(v_alteracoes) > 0 or p_observacao is not null then
    insert into "AnamneseFollowup" (anamnese, observacao, alteracoes)
    values (v_anamnese_id, p_observacao, v_alteracoes);
  end if;

  return jsonb_build_object('anamnese_id', v_anamnese_id, 'alteracoes', v_alteracoes);
end;
$function$;

CREATE OR REPLACE FUNCTION public.agent_registrar_lancamento_despesa(p_whatsapp_number text, p_descricao text, p_valor numeric, p_data date DEFAULT CURRENT_DATE, p_conta_id bigint DEFAULT NULL::bigint, p_consultorio_id bigint DEFAULT NULL::bigint)
 RETURNS bigint
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'public'
AS $function$
declare
  v_owner uuid;
  v_lancamento_id bigint;
begin
  v_owner := public._agent_get_owner_uuid(p_whatsapp_number);

  if p_conta_id is not null and not exists (
    select 1 from "ContaFinanceira" where id = p_conta_id and owner = v_owner
  ) then
    raise exception 'CONTA_INVALIDA' using errcode = 'P0001';
  end if;

  insert into "LancamentoFinanceiro" (data, descricao, valor, tipo, conta, owner)
  values (p_data, p_descricao, p_valor, 'Despesa', p_conta_id, v_owner)
  returning id into v_lancamento_id;

  return v_lancamento_id;
end;
$function$;

CREATE OR REPLACE FUNCTION public.agent_registrar_pagamento_sessao(p_whatsapp_number text, p_sessao_id bigint, p_valor numeric, p_forma_pagamento text, p_conta_id bigint, p_consultorio_id bigint DEFAULT NULL::bigint)
 RETURNS jsonb
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'public'
AS $function$
declare
  v_owner uuid;
  v_sessao_ok bigint;
  v_lancamento_id bigint;
  v_pagamento_id bigint;
begin
  v_owner := public._agent_get_owner_uuid(p_whatsapp_number);

  select s.id into v_sessao_ok
  from "Sessao" s
  where s.id = p_sessao_id and s.owner = v_owner;

  if v_sessao_ok is null then
    raise exception 'SESSAO_NAO_ENCONTRADA' using errcode = 'P0001';
  end if;

  insert into "LancamentoFinanceiro" (data, descricao, valor, tipo, conta, sessao, owner)
  values (current_date, 'Pagamento de sessão', p_valor, 'receita', p_conta_id, p_sessao_id, v_owner)
  returning id into v_lancamento_id;

  insert into "PagamentoSessao" (sessao, valor, data_pagamento, forma_pagamento, conta, lancamento)
  values (p_sessao_id, p_valor, current_date, p_forma_pagamento, p_conta_id, v_lancamento_id)
  returning id into v_pagamento_id;

  update "Sessao"
  set status = 'realizada', "Realizado" = true
  where id = p_sessao_id;

  return jsonb_build_object('pagamento_id', v_pagamento_id, 'lancamento_id', v_lancamento_id);
end;
$function$;

CREATE OR REPLACE FUNCTION public.agent_resumo_financeiro(p_whatsapp_number text, p_data_inicio date, p_data_fim date, p_consultorio_id bigint DEFAULT NULL::bigint)
 RETURNS jsonb
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'public'
AS $function$
declare
  v_owner uuid;
  v_previsto numeric;
  v_realizado numeric;
  v_despesas numeric;
begin
  v_owner := public._agent_get_owner_uuid(p_whatsapp_number);

  select coalesce(sum(p.valor_sessao), 0) into v_previsto
  from "Sessao" s
  join "Paciente" p on p.id = s.paciente
  where s.owner = v_owner
    and s.data between p_data_inicio and p_data_fim;

  select coalesce(sum(total_receita), 0), coalesce(sum(total_despesa), 0)
  into v_realizado, v_despesas
  from v_resumo_financeiro_mensal
  where owner = v_owner
    and to_date(mes_referencia, 'YYYY-MM') between date_trunc('month', p_data_inicio) and date_trunc('month', p_data_fim);

  return jsonb_build_object(
    'previsto', v_previsto,
    'realizado', v_realizado,
    'despesas', v_despesas,
    'saldo', v_realizado - v_despesas
  );
end;
$function$;

CREATE OR REPLACE FUNCTION public.agent_status_pagamento_paciente(p_whatsapp_number text, p_paciente_id bigint, p_consultorio_id bigint DEFAULT NULL::bigint)
 RETURNS TABLE(sessao_id bigint, data date, valor_sessao real, pago boolean, valor_pago real, forma_pagamento text)
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'public'
AS $function$
declare
  v_owner uuid;
begin
  v_owner := public._agent_get_owner_uuid(p_whatsapp_number);

  return query
  select
    s.id,
    s.data,
    coalesce(pac.valor_sessao, 0),
    (pg.id is not null) as pago,
    pg.valor,
    pg.forma_pagamento
  from "Sessao" s
  join "Paciente" pac on pac.id = s.paciente
  left join "PagamentoSessao" pg on pg.sessao = s.id
  where s.owner = v_owner
    and s.paciente = p_paciente_id
  order by s.data desc
  limit 20;
end;
$function$;

-- Remocao das funcoes/coluna que ficam sem nenhum chamador apos a mudanca
-- acima.
DROP FUNCTION IF EXISTS public.agent_definir_consultorio_ativo(text, bigint);
DROP FUNCTION IF EXISTS public.agent_listar_consultorios(text);
DROP FUNCTION IF EXISTS public._agent_resolve_consultorio(text, bigint);

ALTER TABLE agent_sessions DROP COLUMN IF EXISTS consultorio_ativo_id;
```

- [ ] **Step 2: Aplicar a migration**

```bash
node -e "
const { Client } = require('pg');
const fs = require('fs');
(async () => {
  const client = new Client({ connectionString: \`postgresql://postgres:\${process.env.SUPABASE_DB_PASSWORD}@db.rohulajgyxdangxfurha.supabase.co:5432/postgres\` });
  await client.connect();
  await client.query(fs.readFileSync('supabase/migrations/20260827000002_agent_rpc_remove_consultorio_scope.sql', 'utf-8'));
  console.log('OK: migration aplicada');
  await client.end();
})().catch((e) => { console.error(e.message); process.exit(1); });
"
```

- [ ] **Step 3: Verificar contra produção com dado descartável — profissional com 2 consultórios**

```js
// scratch-verificar-agent-owner-scope.js
const fs = require('fs');
function loadEnv(file) {
  const env = {};
  for (const line of fs.readFileSync(file, 'utf-8').split('\n')) {
    const m = line.match(/^([A-Z_]+)=(.*)$/);
    if (m) env[m[1]] = m[2].trim();
  }
  return env;
}
const env = loadEnv('web/.env.local');
const { createClient } = require('./web/node_modules/@supabase/supabase-js');

async function main() {
  const admin = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY);

  // usa o profissional de teste "TESTE" ja usado nas verificacoes do item 10
  // (owner 50b00830-4f45-4918-9b30-13b0b18e5ab3, whatsapp ainda nao vinculado
  // nesse usuario -- ajustar pro whatsapp_number real de teste se for
  // diferente nesta sessao)
  const OWNER = '50b00830-4f45-4918-9b30-13b0b18e5ab3';
  const WHATSAPP_TESTE = '5599999999999';

  await admin.from('Usuarios').update({ whatsapp_number: WHATSAPP_TESTE, whatsapp_verified: true }).eq('id_user', OWNER);

  // cria um segundo consultorio de teste pro mesmo owner
  const { data: consultorioNovo } = await admin.from('Consultorio').insert({
    nome: 'Consultório de teste 2', owner: OWNER, telefone: '11999999999', email_atendimento: 'teste@teste.com',
  }).select('id').single();

  // agent_get_agenda sem p_consultorio_id -- antes lancava CONSULTORIO_AMBIGUO
  // com 2+ consultorios, agora deve so retornar tudo do owner sem erro.
  const { data: agenda, error } = await admin.rpc('agent_get_agenda', {
    p_whatsapp_number: WHATSAPP_TESTE,
    p_data_inicio: '2020-01-01',
    p_data_fim: '2030-01-01',
  });
  console.log('agent_get_agenda sem consultorio ativo:', error ? `ERRO: ${error.message}` : `OK, ${agenda.length} linha(s)`);

  // limpa
  await admin.from('Consultorio').delete().eq('id', consultorioNovo.id);
  await admin.from('Usuarios').update({ whatsapp_number: null, whatsapp_verified: false }).eq('id_user', OWNER);
}
main().catch((e) => { console.error(e.message); process.exit(1); });
```

Esperado: `OK, N linha(s)` sem erro `CONSULTORIO_AMBIGUO` (que não existe mais). Apagar o script depois.

- [ ] **Step 4: Commit**

```bash
git add supabase/migrations/20260827000002_agent_rpc_remove_consultorio_scope.sql
git commit -m "refactor: agente de WhatsApp escopa por owner, nao mais por consultorio ativo"
```

---

## Task 13: Atualizar o workflow n8n do agente

**Files:**
- Modify: `scripts/n8n-agente-whatsapp/03-workflow-agent-psicologo.mjs`

**Interfaces:**
- Nenhuma nova pro código Next.js — output é o workflow reimplantado no n8n via API (fora deste repo).

- [ ] **Step 1: Ler o arquivo e localizar as definições das ferramentas `agent_listar_consultorios` e `agent_definir_consultorio_ativo`**

```bash
grep -n "agent_listar_consultorios\|agent_definir_consultorio_ativo\|CONSULTORIO_AMBIGUO\|qual consultório\|p_consultorio_id" scripts/n8n-agente-whatsapp/03-workflow-agent-psicologo.mjs
```

- [ ] **Step 2: Remover as 2 definições de ferramenta e qualquer trecho do prompt do sistema que instrua o agente a perguntar/tratar `CONSULTORIO_AMBIGUO`**

(o conteúdo exato depende do que o `grep` acima mostrar — remover os blocos de definição de tool das 2 funções descontinuadas e as instruções de prompt correspondentes, sem tocar no restante do workflow.)

- [ ] **Step 3: Reimplantar o workflow via a mesma API do n8n já usada na implantação original**

Seguir o procedimento documentado em `docs/superpowers/plans/2026-08-19-agente-whatsapp-n8n-workflow.md` (mesmo padrão de chamada à API do n8n rodando na VPS, usando as credenciais já em `scripts/n8n-agente-whatsapp/ids.json`).

- [ ] **Step 4: Verificar**

Não é possível testar com mensagem real de WhatsApp (bloqueio já registrado: WhatsApp desconectado, créditos do Gemini esgotados). Verificar apenas que o workflow foi reimplantado sem erro (resposta da API do n8n) e que a definição de ferramentas ativa no workflow não referencia mais as 2 funções removidas (consultar a definição do workflow de volta via API do n8n depois de reimplantar, comparando com o que foi enviado).

- [ ] **Step 5: Commit**

```bash
git add scripts/n8n-agente-whatsapp/03-workflow-agent-psicologo.mjs
git commit -m "refactor: remove ferramentas de consultorio ativo do prompt do agente"
```

---

## Task 14: Registro final — pendências que dependem do usuário

Não é uma task de código — é um checklist de verificação de que tudo que dependia de ação externa está documentado.

- [ ] Confirmar que `ASAAS_API_KEY` e `ASAAS_WEBHOOK_TOKEN` foram preenchidos em produção (EasyPanel) — sem isso, a Task 5 (cliente Asaas) e a Task 7 (webhook) não funcionam de verdade, mesmo com o código certo.
- [ ] Cadastrar o webhook no painel/API do Asaas apontando pra `https://psiagente.com.br/api/asaas/webhook`, com o mesmo valor de `ASAAS_WEBHOOK_TOKEN`.
- [ ] Configurar o job do n8n (novo nó de Cron, mesmo padrão do item 9) pra chamar `POST /api/assinaturas/aplicar-pendencias` 1x/dia com o header `X-Cron-Secret` = `ASSINATURA_CRON_SECRET`.
- [ ] Verificação end-to-end real (checkout → pagamento → webhook → plano liberado) só é possível depois da conta Asaas estar configurada — documentar no `docs/backlog.md` que ficou pendente até lá, igual já foi feito pro item 7 (NFS-e) com a homologação da SEFIN.

---

## Self-Review

**Spec coverage:** gateway Asaas (Task 5-7), planos/preços (Task 3, 6), modelo de dados (Task 1), fluxo de upgrade/downgrade (Task 6-8), inadimplência (Task 7-8), gates de acesso (Task 9-11), refatoração do agente (Task 12-13) — todas as seções da spec têm task correspondente. A trava de coluna (Task 1) e a correção de `alterarPlano` (Task 2) não estavam na spec original — descobertas durante o detalhamento do plano, registradas como achado de segurança.

**Placeholder scan:** nenhum "TBD"/"implementar depois" — os únicos pontos que dependem de ação externa (conta Asaas, reimplantação do n8n) estão marcados como tal explicitamente, com o código em si completo.

**Type consistency:** `PLANOS[usuario.plano]` usado de forma consistente em todos os gates (Task 9-10); `escolherPlano(planoAlvo, prevState, formData)` e `EscolherPlanoForm({ action })` batem entre Task 6 e seu uso na página; `plano_pretendido`/`plano_pretendido_a_partir_de` usados com o mesmo significado em Task 1, 6, 7 e 8.
